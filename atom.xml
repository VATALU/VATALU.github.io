<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>VATALU</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2018-07-29T13:36:33.912Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>VATALU</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Java 函数式编程解析</title>
    <link href="http://yoursite.com/2018/07/27/Java-%E5%87%BD%E6%95%B0%E5%BC%8F%E7%BC%96%E7%A8%8B%E8%A7%A3%E6%9E%90/"/>
    <id>http://yoursite.com/2018/07/27/Java-函数式编程解析/</id>
    <published>2018-07-27T12:55:46.000Z</published>
    <updated>2018-07-29T13:36:33.912Z</updated>
    
    <content type="html"><![CDATA[<div class="note default"><p>前段日子看了看泛型，现在再来看两手 Java 8 引入的函数式编程。</p></div><a id="more"></a><p>Java 的函数式编程是通过 interface 来实现的，对于 Java 这样的语言通过使用 SAM 接口来实现函数式主要是为了简化 Java FP 的入门门槛，Java 并没有采用引入新关键字的方式（例如：C# 引入了 delegation 关键字以及复杂的委托机制），这也使得 Java 当中 <code>函数</code> 不是一等公民，无法脱离 <code>class</code> 的怀抱，这也使得 FP 带来的便捷性受到了一定的损失，（Java 的函数式与真正的函数式语言相比，写起来复杂了很多）通过 SAM 实现函数式是在 Java 环境下的妥协，本文不会对于这一点做过深的讨论。</p><h1 id="Lambda"><a href="#Lambda" class="headerlink" title="Lambda"></a>Lambda</h1><p>函数式总离不开 Lambda 表达式，Lambda 表达式可以替代匿名内部类，以及在标注了函数式接口 <code>@FunctionalInterface</code> 的地方使用。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">()-&gt;System.out.println(<span class="string">"HelloWorld"</span>);</span><br><span class="line">event-&gt;System.out.println(<span class="string">"button clicked"</span>);</span><br><span class="line">(x,y)-&gt;x+y;</span><br><span class="line">(Long x, Long y)-&gt;x+y;</span><br><span class="line">()-&gt;&#123;</span><br><span class="line">    System.out.println(<span class="string">"Hello"</span>);</span><br><span class="line">    System.out.println(<span class="string">"World"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以看到 Lambda 表达式一般是以 ()-&gt;{} 形式，前面填参数，后面填函数语句。Lambda 传进来的参数必须是 final 的或者是既定是不可变的，所以 Lambda 表达式不能改变传进来的参数的值，这也是符合函数式的设计原则的——函数不能有副作用</p><p>前面也提到了 Lambda 可以用在所有传入被 <code>@FunctionalInterface</code> 注解的接口的地方。例如以下形式：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//所定义的函数式接口</span></span><br><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Consumer</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">accept</span><span class="params">(T t)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//在外部写一个函数，函数的参数为此接口。</span></span><br><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">accept</span><span class="params">(<span class="keyword">final</span> T t, <span class="keyword">final</span> Consumer&lt;T&gt; consumer)</span> </span>&#123;</span><br><span class="line">    consumer.accept(t)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//调用这个函数时直接可以传入 Lambda 表达式</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    accept(<span class="string">"HelloWorld"</span>, System.out::println);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>结果输出:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">HelloWorld</span><br></pre></td></tr></table></figure></p><p>上面代码中的 Consumer 接口其实本体位于 JDK 的 java.util.function 包中，JDK 提供了一些基础的函数式接口，下面介绍几个基本的接口。</p><table><thead><tr><th style="text-align:left">接口</th><th style="text-align:left">参数</th><th style="text-align:left">返回类型</th></tr></thead><tbody><tr><td style="text-align:left"><code>Predicate&lt;T&gt;</code></td><td style="text-align:left">T</td><td style="text-align:left">boolean</td></tr><tr><td style="text-align:left"><code>Consumer&lt;T&gt;</code></td><td style="text-align:left">T</td><td style="text-align:left">void</td></tr><tr><td style="text-align:left"><code>Function&lt;T,R&gt;</code></td><td style="text-align:left">T</td><td style="text-align:left">R</td></tr><tr><td style="text-align:left"><code>Supplier&lt;T&gt;</code></td><td style="text-align:left">None</td><td style="text-align:left">T</td></tr><tr><td style="text-align:left"><code>UnaryOperator&lt;T&gt;</code></td><td style="text-align:left">T</td><td style="text-align:left">T</td></tr></tbody></table><p>很多情况下使用这些接口其实就够用了，这里也看到了使用 &lt;&gt; 来进行类型推断，这也是之前 Lambda 例子当中为什么可以不用写类型的原因。</p><h1 id="stream"><a href="#stream" class="headerlink" title="stream"></a>stream</h1><p>Java 8 中更激动人心的特性就是 stream 了，它能够简化 collection 以及 map 的操作，下面给出个 stream 的实例。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">long</span> count  = allArtists.stream()</span><br><span class="line">                        .filter(artist -&gt; artist.isFrom(<span class="string">"London"</span>))</span><br><span class="line">                        .count();</span><br></pre></td></tr></table></figure><p>上述例子计算了所有来自伦敦的艺术家，这里有两个重要的概念引入，一是惰性求值，二十及早求值， filter 当中的 Lambda 表达式时惰性的，只有在调用了及早求值（这里是调用 <code>count()</code> ）时，才会作出计算，例如下面这个例子：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">allArtists.stream()</span><br><span class="line">            .filter(artist -&gt; &#123;</span><br><span class="line">                                System.out.println(<span class="string">"HelloWorld"</span>);</span><br><span class="line">                                artist.isFrom(<span class="string">"London"</span>);</span><br><span class="line">                              &#125;)</span><br></pre></td></tr></table></figure><p>这里并不会输出 <code>HelloWorld</code>，这两个概念在函数式编程语言当中以及习以为常了。 PS:上述的 Lambda 表达式是具有副作用的，它向控制台输出了信息。</p><p>stream 流中封装了很多接口，大部分都是高阶函数（接收或返回一个函数的函数）如 <code>filter</code>, <code>map</code> 等，stream 也对自动装箱做了优化，Integer 对象比 int 基本类型占用了更多的开销，但 stream 仅对整型，长整型和双精度浮点数进行了优化 <code>mapToLong</code>, <code>LongStream</code>。</p><h1 id="函数重载"><a href="#函数重载" class="headerlink" title="函数重载"></a>函数重载</h1><p>调用最具体的那个函数，包括函数前面，以及子类父类之间的关系，下面的例子会输出 <code>IntegerBiFunction</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">overloadedMethod((x,y)-&gt;x+y);</span><br><span class="line"><span class="comment">//两个重载方法</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">overloadedMethod</span><span class="params">(BinaryOperator&lt;Integer&gt;)</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"BinaryOperator"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">overloadedMethod</span><span class="params">(IntegerBiFunction lambda)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"IntegerBiFunction"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是下面的例子就不能通过编译，因为 IntPredicate 和 Predicate 在类型上都是匹配的，而上面这个例子之所以能编译通过是因为 IntegerBiFunction 是 BinaryOperator 的子类。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">overloadedMethod((x,y)-&gt;x+y);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">overloadedMethod</span><span class="params">(Predicate&lt;Integer&gt; predicate)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"Predicate"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">overloadedMethod</span><span class="params">(IntPredicate predicate)</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"IntPredicate"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="默认方法"><a href="#默认方法" class="headerlink" title="默认方法"></a>默认方法</h1><p>Java 8 为了向后兼容，所以在接口中可以定义 <code>default</code> 关键字，即默认方法，默认方法有一套玄学的重载机制…..</p><ul><li>类胜于接口。如果在继承链中有方法或抽象方法的声明，那么就可以忽略接口中定义的方法。</li><li>子类胜于父类。如果一个接口继承了另一个接口，且两个接口都定义了一个默认方法，那么子类中定义的方法胜出。</li><li>没有规则三。如果上面两条都不使用，子类要么需要实现该方法，要么将该方法声明为抽象方法。</li></ul><h1 id="方法引用"><a href="#方法引用" class="headerlink" title="方法引用"></a>方法引用</h1><p>第一个例子已经使用了方法引用 <code>System.out::println</code>, 在所有可以写成 <code>x-&gt;foo(x)</code> 的地方都可以使用 <code>A::foo</code> 替换，这样也使代码更加简洁，对于对象或者是类型名都可以使用。<del>其实这个东西也玄，有序列化和方法引用这个坑</del></p><h1 id="数据分块"><a href="#数据分块" class="headerlink" title="数据分块"></a>数据分块</h1><p>将一个 list 通过某些条件分成两个 list, 大概就是这么做的。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> Map&lt;Boolean, List&lt;Artist&gt;&gt; bandsAndSolo(Stream&lt;Artist&gt; artists) &#123;</span><br><span class="line">    <span class="keyword">return</span> artists.collect(Collectors.partitioningBy(Artist::isSolo));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>partitioninBy 接收一个 Predicate 然后根据每个值传入后输出的布尔值进行二分类。</p><h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>Java 函数式核心库主要的化还有 stream 流的并行还没有讲了，以后用到了的时候再写篇吧，以及 Objects 和 Optional 类以后也讲讲。</p>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note default&quot;&gt;&lt;p&gt;前段日子看了看泛型，现在再来看两手 Java 8 引入的函数式编程。&lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="java" scheme="http://yoursite.com/tags/java/"/>
    
      <category term="Functional Program" scheme="http://yoursite.com/tags/Functional-Program/"/>
    
      <category term="source code" scheme="http://yoursite.com/tags/source-code/"/>
    
  </entry>
  
  <entry>
    <title>WebSocket 调研总结</title>
    <link href="http://yoursite.com/2018/07/23/WebSocket%20%E8%B0%83%E7%A0%94%E6%80%BB%E7%BB%93/"/>
    <id>http://yoursite.com/2018/07/23/WebSocket 调研总结/</id>
    <published>2018-07-23T04:09:09.000Z</published>
    <updated>2018-07-26T03:31:50.530Z</updated>
    
    <content type="html"><![CDATA[<!-- <center> --><div class="note info"><p>本文是将我 confluence 上的文章搬运过来，侧重于前后端实时通讯的横向比较以及 <code>Spring-boot</code> 资源的整理。 </p></div><!-- </center> --><a id="more"></a><h1 id="前后端实时通讯解决方案比较"><a href="#前后端实时通讯解决方案比较" class="headerlink" title="前后端实时通讯解决方案比较"></a>前后端实时通讯解决方案比较</h1><p>Web 为了实现即时通信，有以下几种方案，HTTP short-polling, HTTP long-polling, Streaming, Server-sent Events (SSE), WebSockets。下面会介绍以及比较各种方案的优劣。</p><h2 id="HTTP-short-polling"><a href="#HTTP-short-polling" class="headerlink" title="HTTP short-polling"></a>HTTP short-polling</h2><p>每隔一段时间就自动发出一个 HTTP 请求给服务器。</p><p>这个是最老旧的方式，导致连接数会很多，并且每次发送的请求都会有 HTTP 的 Header，消耗流量。很显然对于服务器的压力大。</p><h2 id="HTTP-long-polling"><a href="#HTTP-long-polling" class="headerlink" title="HTTP long-polling"></a>HTTP long-polling</h2><p>长轮询是对短轮询的改进版，客户端发送 HTTP 给服务器之后，如果没有消息就会阻塞到有消息或者超时了，才会返回给客户端。消息返回之后再建立连接，如此反复。</p><p>在某种程度上减小了网络带宽和 CPU 利用率等问题。这种方式会导致实时性不高，并且网络带宽利用率低的问题也没有从根源上解决。每个 Request 都会带相同的 Header。</p><h2 id="Streaming"><a href="#Streaming" class="headerlink" title="Streaming"></a>Streaming</h2><h3 id="Iframe-Streaming"><a href="#Iframe-Streaming" class="headerlink" title="Iframe Streaming"></a>Iframe Streaming</h3><p>在页面中插入一个隐藏的 iframe, 利用其 src 属性在服务器和客户端之间创建一条长链接，服务器向 iframe 传输数据。</p><p>实现简单，但无法知道连接状态。</p><h3 id="AJAX-multipart-streaming-XHR-Streaming"><a href="#AJAX-multipart-streaming-XHR-Streaming" class="headerlink" title="AJAX multipart streaming (XHR Streaming)"></a>AJAX multipart streaming (XHR Streaming)</h3><p>实现思路：浏览器必须支持 multi-part 标志，客户端通过 AJAX 发出请求 Request，服务器保持住这个连接，然后可以通过 HTTP1.1 的 chunked encoding 机制（分块传输编码）不断 push 数据给客户端,直到 timeout 或者手动断开连接。</p><p>客户端一次连接，服务器数据可多次推送。但是并非所有浏览器都支持 XHR Streaming。</p><h3 id="Flash-Streaming"><a href="#Flash-Streaming" class="headerlink" title="Flash Streaming"></a>Flash Streaming</h3><p>通过内嵌使用了 Socket 类的 Flash 程序，JavaScript 通过调用此 Flash 程序提供的 Socket 接口与服务器端的 Socket 接口进行通信，JavaScript 通过 Flash Socket 接收到服务器端传送的数据。</p><p>缺点很明显，要装 Flash.</p><h2 id="Server-Sent-Events"><a href="#Server-Sent-Events" class="headerlink" title="Server-Sent Events"></a>Server-Sent Events</h2><p>服务器发送事件（SSE）也是 HTML5 公布的一种服务器向浏览器客户端发起数据传输的技术。一旦创建了初始连接，事件流将保持打开状态，直到客户端关闭。该技术通过传统的 HTTP 发送，并具有 WebSockets 缺乏的各种功能，例如自动重新连接、事件 ID 以及发送任意事件的能力。</p><p>SSE 就是利用服务器向客户端声明，接下来要发送的是流信息（streaming），会连续不断地发送过来。这时，客户端不会关闭连接，会一直等着服务器发过来的新的数据流，可以类比视频流。SSE 就是利用这种机制，使用流信息向浏览器推送信息。它基于 HTTP 协议，目前除了 IE/Edge，其他浏览器都支持。</p><p>SSE 是单向通道，只能服务器向浏览器发送，因为流信息本质上就是下载。</p><p>服务器向浏览器发送的 SSE 数据，必须是 UTF-8 编码的文本，具有如下的 HTTP 头信息。</p><pre><code>Content-Type: text/event-streamCache-Control: no-cacheConnection: keep-alive</code></pre><p>必须将 Content-Type 指定为 event-steam。</p><h3 id="SSE-的优势"><a href="#SSE-的优势" class="headerlink" title="SSE 的优势"></a>SSE 的优势</h3><ul><li>通过简单的 HTTP 传输，而不是自定义协议。</li><li>Can be poly-filled with JS to “backport” SSE to browsers that do not support it yet</li><li><p>支持重连</p><h3 id="SSE-的劣势"><a href="#SSE-的劣势" class="headerlink" title="SSE 的劣势"></a>SSE 的劣势</h3></li><li><p>不支持二进制传输</p></li><li>最大连接限制（每个浏览器仅限 6 个，并且不将会在  Chrome 和 Firefox 中修复）</li><li>单向</li></ul><h2 id="WebSocket"><a href="#WebSocket" class="headerlink" title="WebSocket"></a>WebSocket</h2><blockquote><p> WebSocket 是一种在单个 TCP 连接上进行全双工通讯的协议。WebSocket 使得客户端和服务器之间的数据交换变得更加简单，允许服务端主动向客户端推送数据。在 WebSocket API 中，浏览器和服务器只需要完成一次握手，两者之间就直接可以创建持久性的连接，并进行双向数据传输。—— By Wikipedia</p></blockquote><p>过去，创建需要在客户端和服务之间双向通信的web应用，需要通过HTTP来轮询服务器来获取更新然后如果是推送消息则发送另一个请求。这样做会存在一些问题。</p><ul><li>服务器端被迫提供两类接口，一类提供给客户端轮询新消息，一类提供给客户端推送消息给服务器端。</li><li>HTTP协议有较多的额外费用 (overhead)，每次发送消息都会有一个 HTTP header 信息，而且如果不用 Keep-Alive 每次还都要握手。</li><li>客户端的脚本比如JS可能还需要跟踪整个过程，也就是说我发送一个消息后，我可能需要跟踪这个消息的返回。<br>一个简单的办法是使用单个 TCP 连接双向传输。这是为什么提供 WebSocket 协议。与 WebSocket API 结合，它提供了一个替代 HTTP 轮询的方式从 web 页面到远程服务器的双向通信。</li></ul><p>一个简单的办法是使用单个 TCP 连接双向传输。这是为什么提供 WebSocket 协议。与 WebSocket API 结合，它提供了一个替代 HTTP 轮询的方式从 web 页面到远程服务器的双向通信。</p><h2 id="Supported-transports-by-browser-html-served-from-http-or-https"><a href="#Supported-transports-by-browser-html-served-from-http-or-https" class="headerlink" title="Supported transports, by browser (html served from http:// or https://)"></a>Supported transports, by browser (html served from http:// or https://)</h2><table><thead><tr><th><em>Browser</em></th><th><em>Websockets</em></th><th><em>Streaming</em></th><th><em>Polling</em></th></tr></thead><tbody><tr><td>IE 6, 7</td><td>no</td><td>no</td><td>jsonp-polling</td></tr><tr><td>IE 8, 9 (cookies=no)</td><td>no</td><td>xdr-streaming &dagger;</td><td>xdr-polling &dagger;</td></tr><tr><td>IE 8, 9 (cookies=yes)</td><td>no</td><td>iframe-htmlfile</td><td>iframe-xhr-polling</td></tr><tr><td>IE 10</td><td>rfc6455</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Chrome 6-13</td><td>hixie-76</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Chrome 14+</td><td>hybi-10 / rfc6455</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Firefox &lt;10</td><td>no &Dagger;</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Firefox 10+</td><td>hybi-10 / rfc6455</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Safari 5.x</td><td>hixie-76</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Safari 6+</td><td>rfc6455</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Opera 10.70+</td><td>no &Dagger;</td><td>iframe-eventsource</td><td>iframe-xhr-polling</td></tr><tr><td>Opera 12.10+</td><td>rfc6455</td><td>xhr-streaming</td><td>xhr-polling</td></tr><tr><td>Konqueror</td><td>no</td><td>no</td><td>jsonp-polling</td></tr></tbody></table><h2 id="WebSocket-vs-SSE"><a href="#WebSocket-vs-SSE" class="headerlink" title="WebSocket vs SSE"></a>WebSocket vs SSE</h2><p>实际上，所有能够用 SSE 实现的都可以用 WebSockets 替代，WebSockets 受到了更多的青睐并且也有更多的浏览器的支持。</p><p>但是 SSE 可以解决一些特定的业务并且后端也容易实现，SSE 也可以通过 JS polyfilled 不支持 SSE 的老浏览器。详见：Modernizr github page</p><p>所以应该对应不同的场景使用不同的技术。</p><p>SSE 使用场景：</p><ul><li>Stock ticker streaming</li><li>twitter feed updating</li><li>Notifications to browser</li></ul><h1 id="WebSocket-Protocol"><a href="#WebSocket-Protocol" class="headerlink" title="WebSocket Protocol"></a>WebSocket Protocol</h1><p>协议全文详见：<a href="https://tools.ietf.org/html/rfc6455" target="_blank" rel="noopener">RFC 6455</a> </p><p>WebSocket 协议主要分为两个部分，一个是握手的规则，另一个是数据传输。</p><h2 id="握手"><a href="#握手" class="headerlink" title="握手"></a>握手</h2><p>客户端发起请求，采用标准的 HTTP 报文，且仅支持 GET 方法，通过 HTTP Upgrade 机制升级协议至 WebSocket。这样的话只要使用 80/443 端口就可以了。</p><ul><li><p>Sec-WebSocket-Version<br>  客户端表明自己想要使用的版本号（一般都是 13 号版本），如果服务器不支持这个版本，则需要返回自己支持的版本。客户端拿到 Response 以后，需要对自己支持的版本号重新握手。这个 header 客户端必须要发送。</p></li><li><p>Sec-WebSocket-Key</p><p>  客户端请求自动生成的一个 key。这个 header 客户端必须要发送。</p></li><li><p>Sec-WebSocket-Accept</p><p>  服务器针对客户端的 Sec-WebSocket-Key 计算的响应值。这个 header 服务端必须要发送。</p></li><li><p>Sec-WebSocket-Protocol</p><p>  用于协商应用子协议:客户端发送支持的协议列表，服务器必须只回应一个协议名。如果服务器一个协议都不能支持，直接握手失败。客户端可以不发送子协议，但是一旦发送，服务器无法支持其中任意一个都会导致握手失败。这个 header 客户端可选发送。</p></li><li><p>Sec-WebSocket-Extensions</p><p>  用于协商本次连接要使用的 WebSocket 扩展:客户端发送支持的扩展，服务器通过返回相同的首部确认自己支持一或多个扩展。这个 header 客户端可选发送。服务端如果都不支持，不会导致握手失败，但是此次连接不能使用任何扩展。</p></li></ul><h2 id="WebSocket-协议扩展"><a href="#WebSocket-协议扩展" class="headerlink" title="WebSocket 协议扩展"></a>WebSocket 协议扩展</h2><ul><li><p>多路复用扩展(A Multiplexing Extension for WebSockets)<br>这个扩展可以通过握手时建立的 TCP 连接建立多条独立的逻辑上的 WebSocket ( logical connection )通道，每个虚拟的 WebSocket 通道以非零的 ID 标注，0 号通道为控制通道。</p><p>  在 header 里添加 “Sec-WebSocket-Extensions: mux”开启多路复用扩展。在实际上的 WebSocket 连接建立的时候就会自动多开启一条 multiplexed connection。只有客户端通过 AddChannelRequest 才能够创建新的 logical WebSocket 连接。这样接受者就能 ( May ) 并行处理不同通道发送过来的 frames 了。</p><p>  每个通道（除了控制通道）会分配一个定额来限制（默认 64 KB）每个 frame 的 Payload 大小。详文：A Multiplexing Extension for WebSockets</p></li><li><p>压缩扩展(Compression Extensions fos’sr WebSocket)<br>给 WebSocket 协议增加了压缩功能。（例如 x-webkit-deflate-frame 扩展）</p></li></ul><p>如果不进行多路复用扩展，每个 WebSocket 连接都只能独享专门的一个 TCP 连接，而且当遇到一个巨大的消息分成多个帧的时候，容易产生队首阻塞的情况。队首阻塞会导致延迟，所以分成多个帧能尽量的小是关键。不过在进行了多路复用扩展以后，多个连接复用一个 TCP 连接，每个信道依旧会存在队首阻塞的问题。除了多路复用，还要进行多路并行发送消息。</p><p>如果通过 HTTP2 进行 WebSocket 传输，性能会更好一点，毕竟 HTTP2 原生就支持了流的多路复用。利用 HTTP2 的分帧机制进行 WebSocket 的分帧，多个 WebSocket 可以在同一个会话中传输。</p><h2 id="数据格式"><a href="#数据格式" class="headerlink" title="数据格式"></a>数据格式</h2><p>由一个或多个 Frame 组成。</p><p>frame 格式以及每个字段具体含义请参阅 RFC 文档。</p><h2 id="数据传递"><a href="#数据传递" class="headerlink" title="数据传递"></a>数据传递</h2><p>一旦 WebSocket 客户端，服务端建立连接后，后续的操作都是基于数据帧的传递。</p><p>WebSocket 根据 opcode 来区分操作的类型。</p><h2 id="连接保持-心跳"><a href="#连接保持-心跳" class="headerlink" title="连接保持 + 心跳"></a>连接保持 + 心跳</h2><p>客户端与服务端之间长时间没有数据往来，但仍然需要保持连接。这个时候，可以采用心跳来实现。</p><p>ping<br>pong<br>ping, pong 对应的 opcode 分别是 0x9, 0xA。</p><h2 id="关闭连接"><a href="#关闭连接" class="headerlink" title="关闭连接"></a>关闭连接</h2><p>发送或接受一个 close 的 opcode 0x8. 启动 WebSocket 关闭阶段。如果一个终端在收到 close frame 之前没有发出 close frame, 那么这个终端就必须返回一个 close frame. 如果当前有正在传输的 frame, 那么 close frame 将会在这个 frame 传输结束后再发出。<strong>并不能保证终端在发出 close frame 后还能传输数据。</strong></p><h1 id="WebSocket-和-Proxy"><a href="#WebSocket-和-Proxy" class="headerlink" title="WebSocket 和 Proxy"></a>WebSocket 和 Proxy</h1><p>详文：【HTML5 Web Sockets 与代理服务器交互](<a href="http://www.infoq.com/cn/articles/Web-Sockets-Proxy-Servers" target="_blank" rel="noopener">http://www.infoq.com/cn/articles/Web-Sockets-Proxy-Servers</a>)</p><p><img src="https://res.infoq.com/articles/Web-Sockets-Proxy-Servers/en/resources/websockets2.png" alt="proxy"></p><p>可见全部走 secure 肯定是没问题的，使用 https 绕过中间代理，非 https 会导致 http 消息头丢失导致握手升级失败。</p><h1 id="STOMP"><a href="#STOMP" class="headerlink" title="STOMP"></a>STOMP</h1><p>协议详见：<a href="https://stomp.github.io/stomp-specification-1.2.html" target="_blank" rel="noopener">STOMP</a></p><p>STOMP 是属于消息队列的一种协议，很多 MQ 都已经支持。为浏览器和服务器之间的通信增加适当的消息语义。高层协议便于定义应用间所发送消息的语义。WebSocket 协议过于底层，并没有规定 payload 格式，而对于 Pub/Sub 模式，使用 STOMP 最合适不为过了。</p><h2 id="STOMP-Frame-格式"><a href="#STOMP-Frame-格式" class="headerlink" title="STOMP Frame 格式"></a>STOMP Frame 格式</h2><pre><code>COMMANDheader1 : value1header2 : value2Body^@</code></pre><h2 id="STOMP-的优势："><a href="#STOMP-的优势：" class="headerlink" title="STOMP 的优势："></a>STOMP 的优势：</h2><p>Use of STOMP as a sub-protocol enables the Spring Framework and Spring Security to provide a richer programming model vs using raw WebSockets.</p><h1 id="SockJS"><a href="#SockJS" class="headerlink" title="SockJS"></a>SockJS</h1><p>详情：<a href="https://github.com/sockjs/sockjs-client" target="_blank" rel="noopener">SockJs ( Github – The MIT License )</a></p><p>SockJS 是一个 JS 库，它提供了一个类似于 WebSocket 的东西，SockJS 提供了一个连贯的、跨浏览器的 JS API, 他在浏览器和服务器之间创建了一个低延迟、全双工、跨域通信的通道。</p><p>SockJS 的一大好处在于提供了浏览器兼容性，优先使用 Native WebSocket, 在不支持 WebSocket 的浏览器中会自动降为各个浏览器专门的方式（streaming or polling）。一些浏览器缺少对于 WebSocket的支持，因此，回退选项是必要的，Spring 框架也提供了基于 SockJS 协议的透明回退选项。</p><p>SockJS 限制，SockJS 不允许对于一个域名同时打开多个 SockJS 连接。原因是  <a href="https://stackoverflow.com/questions/985431/max-parallel-http-connections-in-a-browser" target="_blank" rel="noopener">browsers don’t allow opening more than two outgoing connections to a single domain</a>. 单个 SockJS  session 需要两个连接——一个是下载数据，一个是发送信息。同时打开第二个 SockJS session 会阻塞。解决方法：使用子域名。</p><p>SockJS 也支持了 multiplex extension ：<a href="https://github.com/sockjs/websocket-multiplex" target="_blank" rel="noopener">sockjs websocket-multiplex</a>, <a href="https://github.com/sockjs/sockjs-client/wiki/How-to-compose-apps-using-WebSockets" target="_blank" rel="noopener">How to do proper multiplexing on WebSockets or on SockJS</a></p><h1 id="Spring"><a href="#Spring" class="headerlink" title="Spring"></a>Spring</h1><p>详文见：<a href="https://docs.spring.io/spring/docs/5.0.0.BUILD-SNAPSHOT/spring-framework-reference/html/websocket.html" target="_blank" rel="noopener">spring framework 5.0.0.BUILD-SNAPSHOT</a></p><p>spring 4.x 就已经支持 WebSocket, STOMP, SockJS.</p><h1 id="Message-Queue"><a href="#Message-Queue" class="headerlink" title="Message Queue"></a>Message Queue</h1><h2 id="RabbitMQ"><a href="#RabbitMQ" class="headerlink" title="RabbitMQ"></a>RabbitMQ</h2><p><a href="https://www.rabbitmq.com/web-stomp.html" target="_blank" rel="noopener">RabbitMQ Web STOMP Plugin</a></p><h2 id="Kafka"><a href="#Kafka" class="headerlink" title="Kafka"></a>Kafka</h2><p>自有一套协议</p><h2 id="ActiveMQ"><a href="#ActiveMQ" class="headerlink" title="ActiveMQ"></a>ActiveMQ</h2><p><a href="http://activemq.apache.org/stomp.html" target="_blank" rel="noopener">ActiveMQ STOMP Protocol Support</a></p>]]></content>
    
    <summary type="html">
    
      &lt;!-- &lt;center&gt; --&gt;
&lt;div class=&quot;note info&quot;&gt;&lt;p&gt;本文是将我 confluence 上的文章搬运过来，侧重于前后端实时通讯的横向比较以及 &lt;code&gt;Spring-boot&lt;/code&gt; 资源的整理。 &lt;/p&gt;&lt;/div&gt;
&lt;!-- &lt;/center&gt; --&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="network" scheme="http://yoursite.com/tags/network/"/>
    
      <category term="WebSocket" scheme="http://yoursite.com/tags/WebSocket/"/>
    
  </entry>
  
  <entry>
    <title>写在明天前</title>
    <link href="http://yoursite.com/2018/07/19/%E5%86%99%E5%9C%A8%E6%98%8E%E5%A4%A9%E5%89%8D/"/>
    <id>http://yoursite.com/2018/07/19/写在明天前/</id>
    <published>2018-07-19T15:32:07.000Z</published>
    <updated>2018-07-23T11:35:44.355Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>实习了快两周了，差不多熟悉了我们组的情况了，想发发牢骚。   </p></div><a id="more"></a><p>先说下技术栈，前端的话基础是 react + node.js, 也算是常用的了，用 yarn 包管理，由于是 Saas 公司，前端必定要用到 Web 3D 那块，WebGL 那块不是很懂。后端的话有远古的 spring framework，也有 spring-boot 1.5, DB 的话 Mysql 主数据库，MongoDB 为辅还有用 Redis 做的队列。然后就是 micro-service，还有 devops 那套运维部署的东西了。</p><p>接触下来周围的人很强，实在是太强了，我们组算上我两个实习生一共 11 个开发，这 9 个人里面竟然有 3 个全栈(本职后端)。组里的业务也是他们还有几位前端佬撑起来的，几位佬也是十分善良的带我这个菜鸡。<br>然后自己这边的工作的话个人觉得比较闲而且根本上线不了，调研 WebSocket. 关于 WS 的协议框架博客也看了七七八八，感觉是一些沟通上的问题以及实在不了解业务场景的问题，感觉进度是慢了点。这两天在看业务代码，感受到了码农的艰辛，“我一急管他呢,随便撸两手代码”，“我不管的，先 run 起来再说”。赶着业务上线，就不能好好写代码，不能好好写代码，就充斥着难以维护的代码，使得整个系统处于危险的悬崖边上。但这也总比为了代码质量而上不了线好。整个行业都是这样的，晚一天上线，有可能就被竞品干掉了。</p><p>最后在抱怨下我所看到的问题吧</p><ul><li>公司处于扩张状态，随着人数的增长，沟通成本大大增加</li><li>整个系统架构（不单单包括我们组的业务）也太不合理了</li><li>敏捷团队不敏捷</li></ul><p>我们组的优势吧在于全栈，像类似 WS 协议层的东西，如果不是前后端都会，是很难推进的。如今前后端完全分离的态势导致了这一层的新技术推进起来困难。所以我是觉得前后端能够使用同一门语言开发是最合适不过的了，Kotlin 就能够做到这一点，前端翻译成 JS,后端翻译 JVM 字节码，然后中间的一些组件能够复用。就能够解决一些问题，把与网络连接的模块交给后端来写，是最合适不过的了，前端就可以更加关注前端的业务逻辑了。（以上是我的设想罢了，Kotlin 的轮子还是要自己造）</p>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;实习了快两周了，差不多熟悉了我们组的情况了，想发发牢骚。   &lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Complaint" scheme="http://yoursite.com/categories/Complaint/"/>
    
    
      <category term="碎碎念" scheme="http://yoursite.com/tags/%E7%A2%8E%E7%A2%8E%E5%BF%B5/"/>
    
  </entry>
  
  <entry>
    <title>写在周一前</title>
    <link href="http://yoursite.com/2018/07/06/%E5%86%99%E5%9C%A8%E5%91%A8%E4%B8%80%E5%89%8D/"/>
    <id>http://yoursite.com/2018/07/06/写在周一前/</id>
    <published>2018-07-06T15:26:52.000Z</published>
    <updated>2018-07-23T11:35:54.116Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>也罢，大二已经结束了，想写点东西。 </p></div><a id="more"></a><p>从小到大，成绩最差的就是语文。无论是家庭环境的影响也好，后天的厌恶也罢，我始终是一个不善于说话的人。随着年龄的增长，这份我不看重的东西使我在很多地方吃了亏。对于我来说，表达出我的思维是个大问题，从语言上理解别人的思维也是个大问题。近段时间的遭遇对我有所打击，因为我发现语言能够将自己那么丁点的学识增加到1.25倍,也能将学识缩小到一半。动动嘴皮子就能看起来很厉害这是最气的。再者就是参加的各种比赛根本就是很可笑的东西，底下的评委不懂技术，只会看看PPT做的好不好，idea好不好，这与我侧重的代码实现，技术选择完全不相干，这也是最气的。</p><p>说实话逃避是很开心的，但实在是太虚无了，颓废的不像个样子。一个人想要废掉，只需要一堵墙和一根网线，如果能够再有个外卖电话就完美了，世界上最远的距离，就是从寝室到楼下拿外卖的距离。就这样子，一寝室四个人，打了半个月的 GTA，想起来还是很骇人的。</p><p>之后就是找实习的事情了，本来投了一两封简历杳无音讯再加上个人本身的确有非常多的东西不理解，很多东西没有学，算法更是什么都不会就打算好好修炼一波明年再说了，但是后来遇到一些贵人，我鬼使神差般的竟然获得了内推，随便摸了两下就过了面试，当面给了offer，下周一就上班了一刚。这结果是我没想到的，实在要多谢那两位贵人，要是网投，早没了，面试也暴露了我很大的问题，写个极其简单的树的算法都写的趔趄，这方面确实差的太多了。</p><p>再就是暑假学车的事情了，我也终于妥协了（大一的时候一直抱着学的太早最后生疏得成为本本族的想法），最后发现驾校根本就是个应试基地，开个倒车还能有口诀，我也是服了，在学生时代还是结束这一大类无聊的活动也好。说实话，开车还是很简单的，和搬砖一样，都是熟练工。</p><p>写下计划吧（制定计划这东西一定是幸存者偏差！），暑假学车，至少科二得过吧，算法么开始啃导论和紫书了，工程上的东西就靠着实习了，中间件的开发还是很憧憬的。</p><p>碎碎念完觉得心情好了好多。</p>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;也罢，大二已经结束了，想写点东西。 &lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Complaint" scheme="http://yoursite.com/categories/Complaint/"/>
    
    
      <category term="碎碎念" scheme="http://yoursite.com/tags/%E7%A2%8E%E7%A2%8E%E5%BF%B5/"/>
    
  </entry>
  
  <entry>
    <title>Servlet, Tomcat, Jetty, Netty, Mina 它们都是什么?</title>
    <link href="http://yoursite.com/2018/06/01/Servlet-Tomcat-Jetty-Netty-Mina-%E5%AE%83%E4%BB%AC%E9%83%BD%E6%98%AF%E4%BB%80%E4%B9%88/"/>
    <id>http://yoursite.com/2018/06/01/Servlet-Tomcat-Jetty-Netty-Mina-它们都是什么/</id>
    <published>2018-06-01T02:44:56.000Z</published>
    <updated>2018-07-26T03:31:36.009Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>这些是 Java Web 当中经常听到的几个名词，那么这些名词的定义是什么？它们之间的关系是什么呢？本文就此展开讨论。</p></div><a id="more"></a><h1 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h1><p>先说结论吧： Servlet是一种Java EE规范，Tomcat &amp; Jetty是Servlet容器，Tomcat包含了Servlet。Servlet本身并不能处理外部请求，需要Servlet容器的配合，Netty和MINA是网络框架，我们可以使用Netty造出自己类似Tomcat的web服务器。简单的关系的话 Tomcat = Jetty &gt; Netty &amp; MINA &gt; Servlet。</p><h1 id="Servlet"><a href="#Servlet" class="headerlink" title="Servlet"></a>Servlet</h1><p> wiki上的定义：</p><blockquote><p>Servlet 是用 Java 编写的服务器端程序。其主要功能在于交互式地浏览和修改数据，生成动态 Web 内容。狭义的 Servlet 是指 Java 语言实现的一个接口，广义的 Servlet 是指任何实现了这个 Servlet 接口的类，一般情况下，人们将 Servlet 理解为后者。</p></blockquote><hr><h1 id="Tomcat"><a href="#Tomcat" class="headerlink" title="Tomcat"></a>Tomcat</h1><p>wiki的定义：</p><blockquote><p>Tomcat 是由 Apache 软件基金会下属的 Jakarta 项目开发的一个 Servlet 容器，按照 Sun Microsystems 提供的技术规范，实现了对 Servlet 和 JavaServer Page（JSP） 的支持，并提供了作为 Web 服务器的一些特有功能，如 Tomcat 管理和控制平台、安全域管理和 Tomcat 阀等。由于 Tomcat 本身也内含了一个 HTTP 服务器，它也可以被视作一个单独的 Web 服务器。但是，不能将 Tomcat 和 Apache HTTP 服务器混淆，Apache HTTP 服务器是一个用C语言实现的 HTTPWeb服务器；这两个 HTTP web server 不是捆绑在一起的。Apache Tomcat 包含了一个配置管理工具，也可以通过编辑XML格式的配置文件来进行配置。</p></blockquote><p>wiki 上跟进的很快也改成 Jakarta 了。</p><hr><h1 id="Jetty"><a href="#Jetty" class="headerlink" title="Jetty"></a>Jetty</h1><p>wiki的定义：</p><blockquote><p>Jetty是一个纯粹的基于Java的网页服务器和Java Servlet容器。尽管网页服务器通常用来为人们呈现文档，但是Jetty通常在较大的软件框架中用于计算机与计算机之间的通信。Jetty作为Eclipse基金会的一部分，是一个自由和开源项目。该网页服务器被用在Apache ActiveMQ[2]、Alfresco[3]、Apache Geronimo[4]、Apache Maven、Apache Spark、Google App Engine[5]、Eclipse[6]、FUSE[7]、Twitter’s Streaming API[8]、Zimbra[9]等产品上。Jetty也是Lift、Eucalyptus、Red5、Hadoop、I2P等开源项目的服务器。[10] Jetty支持最新的Java Servlet API（带JSP的支持），支持SPDY和WebSocket协议。</p></blockquote><hr><h1 id="Netty"><a href="#Netty" class="headerlink" title="Netty"></a>Netty</h1><p>wiki的定义：</p><blockquote><p>Netty is a non-blocking I/O client-server framework for the development of Java network applications such as protocol servers and clients. The asynchronous event-driven network application framework and tools are used to simplify network programming such as TCP and UDP socket servers.[2] Netty includes an implementation of the reactor pattern of programming.</p></blockquote><p>翻译如下：</p><blockquote><p>Netty是一个基于NIO客户端-服务端框架，提供给诸如协议服务端与客户端的Java网络应用。异步事件驱动网络应用框架和工具用来简化TCP和UDP网络编程。Netty包括了一种响应式编程的实现。</p></blockquote><hr><h1 id="Mina"><a href="#Mina" class="headerlink" title="Mina"></a>Mina</h1><p>wiki的定义：</p><blockquote><p>Apache MINA (Multipurpose Infrastructure for Network Applications)[1] is an open source Java network application framework. MINA can be used to create scalable, high performance network applications. MINA provides unified APIs for various transports like TCP, UDP, serial communication. It also makes it easy to make an implementation of custom transport type. MINA provides both high-level and low-level network APIs.</p></blockquote><p>翻译如下</p><blockquote><p>Apache MINA 是一个开源Java网络应用框架。MINA可以创建表现良好的网络应用。MINA为各中通讯机制比如TCP,UDP提供统一的API。并且也可以很方便的实现一个自定义通讯方案。MINA提供高层和底层的API。</p></blockquote><hr><h1 id="关系"><a href="#关系" class="headerlink" title="关系"></a>关系</h1><h2 id="Servlet-一种规范"><a href="#Servlet-一种规范" class="headerlink" title="Servlet 一种规范"></a>Servlet 一种规范</h2><p>Servlet是并不是对网络服务器的封装，而是JEE规范当中的一个。所以它可以支持多层用户协议。</p><h2 id="Tomcat-amp-Jetty-轻量级服务器"><a href="#Tomcat-amp-Jetty-轻量级服务器" class="headerlink" title="Tomcat &amp; Jetty 轻量级服务器"></a>Tomcat &amp; Jetty 轻量级服务器</h2><p>它们俩是同一级别的，都是Servlet容器。对于Servlet容器的工作机制这里有一篇很棒的blog <a href="https://www.ibm.com/developerworks/cn/java/j-lo-servlet/index.html" target="_blank" rel="noopener">Servlet 工作原理解析</a></p><h2 id="Netty-amp-MINA-网络编程框架"><a href="#Netty-amp-MINA-网络编程框架" class="headerlink" title="Netty &amp; MINA 网络编程框架"></a>Netty &amp; MINA 网络编程框架</h2><p>Netty是基于NIO的，Netty也像wiki介绍中说的Netty在很多大型项目中使用，之前文章中的 ElasticSearch transport也使用了Netty。MINA也是NIO框架，和Netty处于同一级别。下面是它们的区别：</p><ol><li>mina比netty出现的早，都是Trustin Lee的作品；</li><li>mina将内核和一些特性的联系过于紧密，使得用户在不需要这些特性3的时候无法脱离，相比下性能会有所下降；netty解决了这个设计问题；</li><li>netty的文档更清晰，很多mina的特性在netty里都有；</li><li>netty更新周期更短，新版本的发布比较快；</li><li>它们的架构差别不大，mina靠apache生存，而netty靠jboss，和jboss的结合度非常高，netty有对google protocal buf的支持，有更完整的ioc容器支持(spring,guice,jbossmc和osgi)；</li><li>netty比mina使用起来更简单，netty里你可以自定义的处理upstream events 或/和 downstream events，可以使用decoder和encoder来解码和编码发送内容；</li><li>netty和mina在处理UDP时有一些不同，netty将UDP无连接的特性暴露出来；而mina对UDP进行了高级层次的抽象，可以把UDP当成”面向连接”的协议，而要netty做到这一点比较困难。mina把TCP和UDP一样当”有连接”的处理，一个UDP请求会按照address产生一个新的 IoSession，过期时间是1分钟，这样做的好处是显然的，但是对于有性能要求的项目就不好了，对一个无连接的东西cache 1分钟，大多数时候可能是白cache了，做无用功。 Mina这样做可能还有个初衷是连续解码用的，比如一个包太大了，分了两次传输；但是这样的设计应该是udp大忌了。</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;这些是 Java Web 当中经常听到的几个名词，那么这些名词的定义是什么？它们之间的关系是什么呢？本文就此展开讨论。&lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="Java" scheme="http://yoursite.com/tags/Java/"/>
    
      <category term="web" scheme="http://yoursite.com/tags/web/"/>
    
      <category term="server" scheme="http://yoursite.com/tags/server/"/>
    
  </entry>
  
  <entry>
    <title>Reactor 3 参考文档阅读笔记</title>
    <link href="http://yoursite.com/2018/05/28/Reactor-3-%E5%8F%82%E8%80%83%E6%96%87%E6%A1%A3%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/"/>
    <id>http://yoursite.com/2018/05/28/Reactor-3-参考文档阅读笔记/</id>
    <published>2018-05-28T02:30:45.000Z</published>
    <updated>2018-07-26T03:30:43.380Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>本文将记录笔者<br><a href="https://htmlpreview.github.io/?https://github.com/get-set/reactor-core/blob/master-zh/src/docs/index.html#about-doc" target="_blank" rel="noopener">Reactor 3 文档中文翻译</a> 的阅读笔记。</p></div><a id="more"></a><h1 id="快速上手"><a href="#快速上手" class="headerlink" title="快速上手"></a>快速上手</h1><p>Flux 用于 N 个元素， Mono用于 0/1 个元素。</p><h1 id="响应式编程"><a href="#响应式编程" class="headerlink" title="响应式编程"></a>响应式编程</h1><h2 id="阻塞是对资源的浪费"><a href="#阻塞是对资源的浪费" class="headerlink" title="阻塞是对资源的浪费"></a>阻塞是对资源的浪费</h2><ol><li>并行化：使用更多的线程和硬件资源</li><li>基于现有的资源来提高效率</li></ol><h2 id="异步可以解决问题吗"><a href="#异步可以解决问题吗" class="headerlink" title="异步可以解决问题吗?"></a>异步可以解决问题吗?</h2><p>两种异步编程方式</p><ol><li>回调 (回调地狱)</li><li>Futures (对于多个处理的组合不好用，get()方法仍然会阻塞且缺乏对多个值以及更进一步对错误的处理。)</li></ol><p><del>反正这里就是说：Reactor一级棒</del></p><h2 id="从命令式编程到响应式编程"><a href="#从命令式编程到响应式编程" class="headerlink" title="从命令式编程到响应式编程"></a>从命令式编程到响应式编程</h2><blockquote><p>Reactor 这样的响应式编程库就是要弥补上述“经典”的 JVM 异步方式所带来的不足。</p></blockquote><ol><li><p>可编排性（Composability） 以及 可读性（Readability）</p></li><li><p>使用丰富的 操作符 来处理形如 流 的数据</p></li><li><p>在 订阅（subscribe） 之前什么都不会发生</p></li><li><p>背压（backpressure） 具体来说即 消费者能够反向告知生产者生产内容的速度的能力</p></li><li><p>高层次 （同时也是有高价值的）的抽象，从而达到 并发无关 的效果</p></li></ol><h3 id="可编排性与可读性"><a href="#可编排性与可读性" class="headerlink" title="可编排性与可读性"></a>可编排性与可读性</h3><blockquote><p>可编排性，指的是编排多个异步任务的能力。比如我们将前一个任务的结果传递给后一个任务作为输入， 或者将多个任务以分解再汇总（fork-join）的形式执行，或者将异步的任务作为离散的组件在系统中 进行重用。</p></blockquote><blockquote><p>Reactor 提供了丰富的编排操作，从而代码直观反映了处理流程，并且所有的操作保持在同一层次 （尽量避免了嵌套）。</p></blockquote><h3 id="就像装配流水线"><a href="#就像装配流水线" class="headerlink" title="就像装配流水线"></a>就像装配流水线</h3><blockquote><p>你可以想象数据在响应式应用中的处理，就像流过一条装配流水线。Reactor 既是传送带， 又是一个个的装配工或机器人。原材料从源头（最初的 Publisher）流出，最终被加工为成品， 等待被推送到消费者（或者说 Subscriber）。</p><p>原材料会经过不同的中间处理过程，或者作为半成品与其他半成品进行组装。如果某处有齿轮卡住， 或者某件产品的包装过程花费了太久时间，相应的工位就可以向上游发出信号来限制或停止发出原材料。</p></blockquote><p>这两段比喻可以说时很形象了，从代码角度.两下就完事了。</p><h3 id="操作符"><a href="#操作符" class="headerlink" title="操作符"></a>操作符</h3><blockquote><p>在 Reactor 中，操作符（operator）就像装配线中的工位（操作员或装配机器人）。每一个操作符 对 Publisher 进行相应的处理，然后将 Publisher 包装为一个新的 Publisher。就像一个链条， 数据源自第一个 Publisher，然后顺链条而下，在每个环节进行相应的处理。最终，一个订阅者 (Subscriber）终结这个过程。请记住，在订阅者（Subscriber）订阅（subscribe）到一个 发布者（Publisher）之前，什么都不会发生。</p></blockquote><h3 id="subscribe-之前什么都不会发生"><a href="#subscribe-之前什么都不会发生" class="headerlink" title="subscribe()之前什么都不会发生"></a>subscribe()之前什么都不会发生</h3><blockquote><p>在 Reactor 中，当你创建了一条 Publisher 处理链，数据还不会开始生成。事实上，你是创建了 一种抽象的对于异步处理流程的描述（从而方便重用和组装）。</p></blockquote><p>当真正“订阅（subscrib）”的时候，你需要将 Publisher 关联到一个 Subscriber 上，然后 才会触发整个链的流动。这时候，Subscriber 会向上游发送一个 request 信号，一直到达源头 的 Publisher。</p><h3 id="背压"><a href="#背压" class="headerlink" title="背压"></a>背压</h3><blockquote><p>向上游传递信号这一点也被用于实现 背压 ，就像在装配线上，某个工位的处理速度如果慢于流水线 速度，会对上游发送反馈信号一样。</p></blockquote><h3 id="热-vs-冷"><a href="#热-vs-冷" class="headerlink" title="热 vs 冷"></a>热 vs 冷</h3><p>在 Rx 家族的响应式库中，响应式流分为“热”和“冷”两种类型，区别主要在于响应式流如何 对订阅者进行响应：</p><ol><li><p>一个“冷”的序列，指对于每一个 Subscriber，都会收到从头开始所有的数据。如果源头 生成了一个 HTTP 请求，对于每一个订阅都会创建一个新的 HTTP 请求。</p></li><li><p>一个“热”的序列，指对于一个 Subscriber，只能获取从它开始 订阅 之后 发出的数据。不过注意，有些“热”的响应式流可以缓存部分或全部历史数据。 通常意义上来说，一个“热”的响应式流，甚至在即使没有订阅者接收数据的情况下，也可以 发出数据（这一点同 “Subscribe() 之前什么都不会发生”的规则有冲突）。</p></li></ol><h1 id="Reactor核心特性"><a href="#Reactor核心特性" class="headerlink" title="Reactor核心特性"></a>Reactor核心特性</h1><blockquote><p>Reactor 项目的主要 artifact 是 reactor-core，这是一个基于 Java 8 的实现了响应式流规范 （Reactive Streams specification）的响应式库。</p></blockquote><h2 id="简单的创建和订阅Flux或Mono的方法"><a href="#简单的创建和订阅Flux或Mono的方法" class="headerlink" title="简单的创建和订阅Flux或Mono的方法"></a>简单的创建和订阅Flux或Mono的方法</h2><p>Flux 和 Mono 提供的工厂方法<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Flux&lt;String&gt; seq1 = Flux.just(<span class="string">"foo"</span>, <span class="string">"bar"</span>, <span class="string">"foobar"</span>);</span><br><span class="line"></span><br><span class="line">List&lt;String&gt; iterable = Arrays.asList(<span class="string">"foo"</span>, <span class="string">"bar"</span>, <span class="string">"foobar"</span>);</span><br><span class="line">Flux&lt;String&gt; seq2 = Flux.fromIterable(iterable);</span><br><span class="line">Mono&lt;String&gt; noData = Mono.empty(); </span><br><span class="line"></span><br><span class="line">Mono&lt;String&gt; data = Mono.just(<span class="string">"foo"</span>);</span><br><span class="line"></span><br><span class="line">Flux&lt;Integer&gt; numbersFromFiveToSeven = Flux.range(<span class="number">5</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure></p><p>基于lambda的对Flux的订阅</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">subscribe();</span><br></pre></td></tr></table></figure><p>订阅并触发序列</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">subscribe(Consumer&lt;? <span class="keyword">super</span> T&gt; consumer);</span><br></pre></td></tr></table></figure><p>对每个生成的元素进行消费</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">subscribe(Consumer&lt;? <span class="keyword">super</span> T&gt; consumer,</span><br><span class="line">        Consumer&lt;? <span class="keyword">super</span> Throwable&gt; errorConsumer);</span><br></pre></td></tr></table></figure><p>对正常元素进行消费，也对错误进行响应。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">subscribe(Consumer&lt;? <span class="keyword">super</span> T&gt; consumer,</span><br><span class="line">        Consumer&lt;? <span class="keyword">super</span> Throwable&gt; errorConsumer,</span><br><span class="line">        Runnable completeConsumer);</span><br></pre></td></tr></table></figure><p>对正常元素和错误均有响应，还定义了序列正常完成后的回调</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">subscribe(Consumer&lt;? <span class="keyword">super</span> T&gt; consumer,</span><br><span class="line">        Consumer&lt;? <span class="keyword">super</span> Throwable&gt; errorConsumer,</span><br><span class="line">        Runnable completeConsumer,</span><br><span class="line">        Consumer&lt;? <span class="keyword">super</span> Subscription&gt; subscriptionConsumer);</span><br></pre></td></tr></table></figure><p>对正常元素、错误和完成信号均有响应，同时也定义了对该subscribe方法返回的Subscription执行的回调</p><blockquote><p>以上方法会返回一个 Subscription 的引用，如果不再需要更多元素你可以通过它来取消订阅。 取消订阅时， 源头会停止生成新的数据，并清理相关资源。取消和清理的操作在 Reactor 中是在 接口 Disposable 中定义的。</p></blockquote><h3 id="subscribe方法示例"><a href="#subscribe方法示例" class="headerlink" title="subscribe方法示例"></a>subscribe方法示例</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Flux&lt;Integer&gt; ints = Flux.range(<span class="number">1</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure><p>配置一个在订阅的时候产生3个值得Flux。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ints.subscribe();</span><br></pre></td></tr></table></figure><p>最简单得订阅方式。</p><blockquote><p>第二行代码没有任何输出，但是它确实执行了。Flux 产生了3个值。如果我们传入一个 lambda， 我们就可以看到这几个值，如下一个列子：</p></blockquote><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Flux&lt;Integer&gt; ints = Flux.range(<span class="number">1</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure><p>配置一个在订阅时会产生3个值得Flux。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ints.subscribe(i -&gt; System.out.println(i));</span><br></pre></td></tr></table></figure><p>订阅它并打印值。</p><p>== 待续 ==</p>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;本文将记录笔者&lt;br&gt;&lt;a href=&quot;https://htmlpreview.github.io/?https://github.com/get-set/reactor-core/blob/master-zh/src/docs/index.html#about-doc&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Reactor 3 文档中文翻译&lt;/a&gt; 的阅读笔记。&lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="Functional Program" scheme="http://yoursite.com/tags/Functional-Program/"/>
    
      <category term="Reactor" scheme="http://yoursite.com/tags/Reactor/"/>
    
      <category term="Java" scheme="http://yoursite.com/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>Java 一组常量应该如何存储？</title>
    <link href="http://yoursite.com/2018/05/28/Java-%E4%B8%80%E7%BB%84%E5%B8%B8%E9%87%8F%E5%BA%94%E8%AF%A5%E5%A6%82%E4%BD%95%E5%AD%98%E5%82%A8%EF%BC%9F/"/>
    <id>http://yoursite.com/2018/05/28/Java-一组常量应该如何存储？/</id>
    <published>2018-05-28T01:13:15.000Z</published>
    <updated>2018-07-26T03:30:46.616Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>在 Java 当中用哪种方式储存一组常量比较合理呢？是使用常量接口、常量类还是枚举类型呢？ </p></div><a id="more"></a><p>先说结论：定义常量不要使用接口常量，要在类中定义，使用 final 类以及 private 构造方法，如果常量可以进行归类，最好使用枚举定义：枚举 &gt; 类 &gt; 接口。</p><p>第一种：使用接口</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Constants</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> AUDIT_STATUS_PASS = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> AUDIT_STATUS_NOT_PASS = <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用接口来保存常量的话会产生一些问题，常量接口是一种严重的反模式行为，《Effective Java》这么写道：</p><blockquote><p>The constant interface pattern is a poor use of interfaces. That a class uses some constants internally is an implementation detail. Implementing a constant interface causes this implementation detail to leak into the class’s exported API. It is of no consequence to the users of a class that the class implements a constant interface. In fact, it may even confuse them. Worse, it represents a commitment: if in a future release the class is modified so that it no longer needs to use the constants, it still must implement the interface to ensure binary compatibility. If a nonfinal class implements a constant interface, all of its subclasses will have their namespaces polluted by the constants in the interface.</p></blockquote><p>翻译如下：</p><blockquote><p>常量接口模式是对接口的不良使用。类在内部使用某些常量，这纯粹是实现细节。实现常量接口会导致把这样的实现细节泄露到该类的导出API中。类实现常量接口，这对于类的用户来讲并没有什么价值。实际上，这样做反而会使他们更加糊涂。更糟糕的是，它代表了一种承诺：如果在将来的发行版本中，这个类被修改了，它不再需要使用这些常量了，它依然必须实现这个接口，以确保兼容性。如果非final类实现了常量接口，它的所有子类的命名空间也会被接口中的常量所“污染”。</p></blockquote><p>总结下：</p><ol><li><p>接口是不能阻止被实现或继承的，也就是说子接口或实现中是能够覆盖掉常量的定义，这样通过父，子接口(或实现) 去引用常量是可能不一致的</p></li><li><p>同样的，由于被实现或继承，造成在继承树中可以用大量的接口, 类 或实例去引用 同一个常量，从而造成接口中定义的常量污染了命名空间。(Java 编译器竟然允许使用实例去引用类变量)</p></li><li><p>接口暗含的意思是：它是需被实现的，代表着一种类型，它的公有成员是要被暴露的 API。而在接口中定义的常量说不上是 API</p></li></ol><hr><p>第二种：使用类</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Constans</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> AUDIT_STATUS_PASS = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> AUDIT_STATUS_NOT_PASS = <span class="number">2</span>;</span><br><span class="line">    <span class="comment">//私有的构造方法</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Constans</span><span class="params">()</span></span>&#123;&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>添加 final 关键字来避免被继承，隐藏构造方法防止实例化，常量类解决了这两个问题，常量类会比常量接口编译出来的源文件字节多一点点。</p><hr><p>第三种：使用枚举</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Constants &#123;</span><br><span class="line">    AUDIT_STATUS_PASS(<span class="number">1</span>),</span><br><span class="line">    AUDIT_STATUS_NOT_PASS(<span class="number">2</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> status;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Constants</span><span class="params">(<span class="keyword">int</span> status)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.setStatus(status);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getStatus</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> status;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setStatus</span><span class="params">(<span class="keyword">int</span> status)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.status = status;</span><br><span class="line">    &#125;</span><br><span class="line">        </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>《Effective Java》中有这么一个例子</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> APPLE_FUJI = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> APPLE_PIPPIN = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> APPLE_GRANNY_SMITH = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> ORANGE_NAVEL = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> ORANGE_TEMPLE = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> ORANGE_BLOOD = <span class="number">2</span>;</span><br></pre></td></tr></table></figure><p>针对int常量以下不足： </p><ol><li>在类型安全方面，如果你想使用的是ORANGE_NAVEL，但是传递的是APPLE_FUJI，编译器并不能检测出错误； </li><li>因为int常量是编译时常量，被编译到使用它们的客户端中。若与枚举常量关联的int发生了变化，客户端需重新编译，否则它们的行为就不确定； </li><li>没有便利方法将int常量翻译成可打印的字符串。这里的意思应该是比如你想调用的是ORANGE_NAVEL，debug的时候显示的是0，但你不能确定是APPLE_FUJI还是ORANGE_NAVEL</li></ol><p>如果你想使用String常量，虽然提供了可打印的字符串，但是性能会有影响。特殊是对于有些新手开发，有可能会直接将String常量硬编码到代码中，导致以后修改很困难。</p><p>所有这一切，enum都给出了具体的解决。唯一的缺点只是需要增加enum加载和实例化的时间。</p><p>以上面的 APPLE,ORANGE为例，用 enum 重写：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">针对<span class="keyword">int</span>常量以下不足： </span><br><span class="line"><span class="number">1</span>. 在类型安全方面，如果你想使用的是 ORANGE_NAVEL，但是传递的是 APPLE_FUJI，编译器并不能检测出错误； </span><br><span class="line"><span class="number">2</span>. 因为 <span class="keyword">int</span> 常量是编译时常量，被编译到使用它们的客户端中。若与枚举常量关联的 <span class="keyword">int</span> 发生了变化，客户端需重新编译，否则它们的行为就不确定； </span><br><span class="line"><span class="number">3</span>. 没有便利方法将 <span class="keyword">int</span> 常量翻译成可打印的字符串。这里的意思应该是比如你想调用的是 ORANGE_NAVEL，debug 的时候显示的是 <span class="number">0</span>，但你不能确定是 APPLE_FUJI 还是 ORANGE_NAVEL</span><br></pre></td></tr></table></figure><pre><code>如果你想使用 String 常量，虽然提供了可打印的字符串，但是性</code></pre><p>能会有影响。特殊是对于有些新手开发，有可能会直接将String常量硬编码到代码中，导致以后修改很困难。<br>    所有这一切，enum 都给出了具体的解决。唯一的缺点只是需要增加enum 加载和实例化的时间。</p>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;在 Java 当中用哪种方式储存一组常量比较合理呢？是使用常量接口、常量类还是枚举类型呢？ &lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="Java" scheme="http://yoursite.com/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>IPv6 with Docker中文翻译</title>
    <link href="http://yoursite.com/2018/01/30/IPv6-with-Docker%E4%B8%AD%E6%96%87%E7%BF%BB%E8%AF%91/"/>
    <id>http://yoursite.com/2018/01/30/IPv6-with-Docker中文翻译/</id>
    <published>2018-01-29T16:07:55.000Z</published>
    <updated>2018-07-26T03:31:06.863Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>老师的项目要在Ipv6环境下跑，结果发现中文版却并没有对此篇文章的翻译，本瓜皮就来翻译一下。<br><a href="https://docs.docker.com/engine/userguide/networking/default_network/ipv6/" target="_blank" rel="noopener">原文地址</a>  </p></div><a id="more"></a><blockquote><p>The information in this section explains IPv6 with the Docker default bridge. This is a bridge network named bridge created automatically when you install Docker.</p></blockquote><p>这篇文章通过 Docker default bridge 来解释 IPv6 。这是一个 bridge 网络在你安装 Docker 的时候就自动创建。</p><blockquote><p>As we are running out of IPv4 addresses the IETF has standardized an IPv4 successor, Internet Protocol Version 6 , in RFC 2460. Both protocols, IPv4 and IPv6, reside on layer 3 of the OSI model.</p></blockquote><p>当我们的 IPv4 地址用尽的时候，IETF 在 RFC 2460 已经制定了一个 IPv4 的继承者———— IPv6，这两个协议都位于 OSI model(Open Systems Interconnection model) 的第三层。</p><h1 id="How-IPv6-works-on-Docker"><a href="#How-IPv6-works-on-Docker" class="headerlink" title="How IPv6 works on Docker"></a>How IPv6 works on Docker</h1><blockquote><p>By default, the Docker server configures the container network for IPv4 only. You can enable IPv4/IPv6 dualstack support by running the Docker daemon with the –ipv6 flag. Docker will set up the bridge docker0 with the IPv6 link-local address fe80::1.</p></blockquote><p>默认情况下， Docker server 只配置了这个容器的 IPv4 网络. 你可以同时使用 IPv4/IPv6 通过添加 –ipv6 执行 Docker 进程 。Docker 会设置 bridge docker0 通过 IPv6 链接本地地址 fe80::1。</p><blockquote><p>By default, containers that are created will only get a link-local IPv6 address. To assign globally routable IPv6 addresses to your containers you have to specify an IPv6 subnet to pick the addresses from. Set the IPv6 subnet via the –fixed-cidr-v6 parameter when starting Docker daemon:</p></blockquote><p>默认情况下，创建的容器只会得到一个链接本地 Ipv6 地址。为了能够分配可全球路由的 IPv6 地址给你的容器，你不得不指定一个 IPv6 子网去建立地址表。当 Docker 进程启动时，通过 –fixed-cidr-v6 参数设置 IPv6 子网。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dockerd --ipv6 --fixed-cidr-v6="2001:db8:1::/64"</span><br></pre></td></tr></table></figure><blockquote><p>The subnet for Docker containers should at least have a size of /80. This way an IPv6 address can end with the container’s MAC address and you prevent NDP neighbor cache invalidation issues in the Docker layer.</p></blockquote><p>Docker 容器的子网至少具有 /80 的形式。这样 IPv6 地址就能够以容器的 MAC 地址结尾并且防止在 Docker 层 NDP 缓存失效问题。</p><blockquote><p>With the –fixed-cidr-v6 parameter set Docker will add a new route to the routing table. Further IPv6 routing will be enabled (you may prevent this by starting dockerd with –ip-forward=false):</p></blockquote><p>通过 –fixed-cidr-v6 参数设置 Docker 会在路由表增加一个新路由。更多的 IPv6 路由将会启用（你通过用 –ip-forward=false 启动 Docker 来防止这种事情的发生）</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> ip -6 route add 2001:db8:1::/64 dev docker0</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span> sysctl net.ipv6.conf.default.forwarding=1</span><br><span class="line"></span><br><span class="line"><span class="meta">$</span> sysctl net.ipv6.conf.all.forwarding=1</span><br></pre></td></tr></table></figure><blockquote><p>All traffic to the subnet 2001:db8:1::/64 will now be routed via the docker0 interface.</p></blockquote><p>所有到 2001:db8:1::/64 子网的路由都会通过 docker0 接口路由。</p><blockquote><p>Be aware that IPv6 forwarding may interfere with your existing IPv6 configuration: If you are using Router Advertisements to get IPv6 settings for your host’s interfaces you should set accept_ra to 2. Otherwise IPv6 enabled forwarding will result in rejecting Router Advertisements. E.g., if you want to configure eth0 via Router Advertisements you should set:</p></blockquote><p>请注意 IPv6 转发会与 现存的 IPv6 配置连接：如果你正使用路由器广播来为你的主机接口获得 IPv6 设置，你应该设置 accept_ra=2 。否则 IPv6 转发会导致拒绝路由器广播。比如，如果你想配置 eth0 通过路由器广播，你应该这么设置。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> sysctl net.ipv6.conf.eth0.accept_ra=2</span><br></pre></td></tr></table></figure><blockquote><p>Every new container will get an IPv6 address from the defined subnet. Further a default route will be added on eth0 in the container via the address specified by the daemon option –default-gateway-v6 if present, otherwise via fe80::1:</p></blockquote><p>所有的容器都会从定义的子网获得一个 IPv6 地址。一个默认的路由将会增加到 eth0 如果存在进程选项 –default-gateway-v6，通过被特定化的地址，否则默认通过 fe80::1: </p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">docker run -it ubuntu bash -c "ip -6 addr show dev eth0; ip -6 route show"</span><br><span class="line"></span><br><span class="line">15: eth0: &lt;BROADCAST,UP,LOWER_UP&gt; mtu 1500</span><br><span class="line">   inet6 2001:db8:1:0:0:242:ac11:3/64 scope global</span><br><span class="line">      valid_lft forever preferred_lft forever</span><br><span class="line">   inet6 fe80::42:acff:fe11:3/64 scope link</span><br><span class="line">      valid_lft forever preferred_lft forever</span><br><span class="line"></span><br><span class="line">2001:db8:1::/64 dev eth0  proto kernel  metric 256</span><br><span class="line">fe80::/64 dev eth0  proto kernel  metric 256</span><br><span class="line">default via fe80::1 dev eth0  metric 1024</span><br></pre></td></tr></table></figure><blockquote><p>In this example the Docker container is assigned a link-local address with the network suffix /64 (here: fe80::42:acff:fe11:3/64) and a globally routable IPv6 address (here: 2001:db8:1:0:0:242:ac11:3/64). The container will create connections to addresses outside of the 2001:db8:1::/64 network via the link-local gateway at fe80::1 on eth0.</p></blockquote><p>在这个例子当中 Docker 容器通过网络后缀 /64 (here: fe80::42:acff:fe11:3/64)和一个全局 IPv6 路由地址(here: 2001:db8:1:0:0:242:ac11:3/64)被分配了一个本地链接地址。通过这个本地链接网关 fe80::1，这个容器将会创造通往 2001:db8:1::/64 之外的网络地址连接。</p><blockquote><p>Often servers or virtual machines get a /64 IPv6 subnet assigned (e.g. 2001:db8:23:42::/64). In this case you can split it up further and provide Docker a /80 subnet while using a separate /80 subnet for other applications on the host:</p></blockquote><p>通常服务器或者虚拟机得到一个 /64 IPv6 子网分配 (比如： 2001:db8:23:42::/64)。当你想要给别的主机上的应用使用单独的 /80 子网，你可以分开它并提供给 Docker /80 子网</p><blockquote><p>In this setup the subnet 2001:db8:23:42::/64 with a range from 2001:db8:23:42:0:0:0:0 to 2001:db8:23:42:ffff:ffff:ffff:ffff is attached to eth0, with the host listening at 2001:db8:23:42::1. The subnet 2001:db8:23:42:1::/80 with an address range from 2001:db8:23:42:1:0:0:0 to 2001:db8:23:42:1:ffff:ffff:ffff is attached to docker0 and will be used by containers.</p></blockquote><p> 2001:db8:23:42::/64 子网从  2001:db8:23:42:0:0:0:0 到 2001:db8:23:42:ffff:ffff:ffff:ffff 被赋给 eth0，在 2001:db8:23:42::1 被监听。子网 2001:db8:23:42:1::/80 从 2001:db8:23:42:1:0:0:0 to 2001:db8:23:42:1:ffff:ffff:ffff 将会被赋给 docker0 并且将会被容器使用。</p><h2 id="Using-NDP-proxying"><a href="#Using-NDP-proxying" class="headerlink" title="Using NDP proxying"></a>Using NDP proxying</h2><blockquote><p>If your Docker host is the only part of an IPv6 subnet but does not have an IPv6 subnet assigned, you can use NDP proxying to connect your containers to the internet via IPv6. If the host with IPv6 address 2001:db8::c001 is part of the subnet 2001:db8::/64 and your IaaS provider allows you to configure the IPv6 addresses 2001:db8::c000 to 2001:db8::c00f, your network configuration may look like the following:</p></blockquote><p>如果你的 Docker 主机仅是唯一的 IPv6 子网，没有 IPv6 子网被分配，你可以用 NDP peoxying 去连接你的容器到网络。如果主机有着 2001:db8::c001 地址是 2001:db8::/64 的一部分并且你的 IaaS 提供商允许你配置 IPv6 地址 2001:db8::c000 ~ 2001:db8::c00f，你的网络配置应像下面一样。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> ip -6 addr show</span><br><span class="line"></span><br><span class="line">1: lo: &lt;LOOPBACK,UP,LOWER_UP&gt; mtu 65536</span><br><span class="line">    inet6 ::1/128 scope host</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br><span class="line">2: eth0: &lt;BROADCAST,MULTICAST,UP,LOWER_UP&gt; mtu 1500 qlen 1000</span><br><span class="line">    inet6 2001:db8::c001/64 scope global</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br><span class="line">    inet6 fe80::601:3fff:fea1:9c01/64 scope link</span><br><span class="line">       valid_lft forever preferred_lft forever</span><br></pre></td></tr></table></figure><blockquote><p>To slit up the configurable address range into two subnets 2001:db8::c000/125 and 2001:db8::c008/125, use the following daemon.json settings. The first subnet is used by non-Docker processes on the host, and the second is used by Docker.</p></blockquote><p>将配置的地址分开成  2001:db8::c000/125 与 2001:db8::c008/125，用下面的 daemon.json。第一个子网会被用在主机上，而第二个会被 Docker 使用。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"ipv6"</span>: <span class="literal">true</span>,</span><br><span class="line">  <span class="attr">"fixed-cidr-v6"</span>: <span class="string">"2001:db8::c008/125"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>The Docker subnet is within the subnet managed by your router and connected to eth0. All containers with addresses assigned by Docker are expected to be found within the router subnet, and the router can communicate with these containers directly.</p></blockquote><p>Docker 子网被路由管理并且连接到 eth0。所有的具有这个地址的容器被  Docker 分配并且会在路由的子网里被找到。路由器可以和这些容器直接通信。</p><h2 id="IPv6-NDP-proxying"><a href="#IPv6-NDP-proxying" class="headerlink" title="IPv6 NDP proxying"></a>IPv6 NDP proxying</h2><blockquote><p>When the router wants to send an IPv6 packet to the first container, it transmits a neighbor solicitation request, asking “Who has 2001:db8::c009?” However, no host on the subnet has the address; the container with the address is hidden behind the Docker host. The Docker host therefore must listen for neighbor solicitation requests and respond that it is the device with the address. This functionality is called the NDP Proxy and is handled by the kernel on the host machine. To enable the NDP proxy, execute the following command:</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> sysctl net.ipv6.conf.eth0.proxy_ndp=1</span><br></pre></td></tr></table></figure><blockquote><p>Next, add the container’s IPv6 address to the NDP proxy table:</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> ip -6 neigh add proxy 2001:db8::c009 dev eth0</span><br></pre></td></tr></table></figure><blockquote><p>From now on, the kernel answers neighbor solicitation addresses for this address on the device eth0. All traffic to this IPv6 address is routed through the Docker host, which forwards it to the container’s network according to its routing table via the docker0 device:</p></blockquote><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">$</span> ip -6 route show</span><br><span class="line"></span><br><span class="line">2001:db8::c008/125 dev docker0  metric 1</span><br><span class="line">2001:db8::/64 dev eth0  proto kernel  metric 256</span><br></pre></td></tr></table></figure><blockquote><p>Execute the ip -6 neigh add proxy … command for every IPv6 address in your Docker subnet. Unfortunately there is no functionality for adding a whole subnet by executing one command. An alternative approach would be to use an NDP proxy daemon such as ndppd.</p></blockquote><h1 id="Docker-IPv6-cluster"><a href="#Docker-IPv6-cluster" class="headerlink" title="Docker IPv6 cluster"></a>Docker IPv6 cluster</h1><h2 id="Switched-network-environment"><a href="#Switched-network-environment" class="headerlink" title="Switched network environment"></a>Switched network environment</h2><blockquote><p>Using routable IPv6 addresses allows you to realize communication between containers on different hosts. Let’s have a look at a simple Docker IPv6 cluster example:</p></blockquote><p>IPv6 switched network example</p><blockquote><p>The Docker hosts are in the 2001:db8:0::/64 subnet. Host1 is configured to provide addresses from the 2001:db8:1::/64 subnet to its containers. It has three routes configured:</p></blockquote><ul><li>Route all traffic to 2001:db8:0::/64 via eth0</li><li>Route all traffic to 2001:db8:1::/64 via docker0</li><li>Route all traffic to 2001:db8:2::/64 via Host2 with IP 2001:db8::2</li></ul><blockquote><p>Host1 also acts as a router on OSI layer 3. When one of the network clients tries to contact a target that is specified in Host1’s routing table Host1 forwards the traffic accordingly. It acts as a router for all networks it knows: 2001:db8::/64, 2001:db8:1::/64, and 2001:db8:2::/64.</p></blockquote><blockquote><p>On Host2 we have nearly the same configuration. Host2’s containers gets IPv6 addresses from 2001:db8:2::/64. Host2 has three routes configured:</p></blockquote><ul><li>Route all traffic to 2001:db8:0::/64 via eth0</li><li>Route all traffic to 2001:db8:2::/64 via docker0</li><li>Route all traffic to 2001:db8:1::/64 via Host1 with IP 2001:db8:0::1</li></ul><blockquote><p>The difference to Host1 is that the network 2001:db8:2::/64 is directly attached to Host2 via its docker0 interface whereas Host2 reaches 2001:db8:1::/64 via Host1’s IPv6 address 2001:db8::1.</p></blockquote><blockquote><p>This way every container can contact every other container. The containers Container1-<em> share the same subnet and contact each other directly. The traffic between Container1-</em> and Container2-* are routed via Host1 and Host2 because those containers do not share the same subnet.</p></blockquote><blockquote><p>In a switched environment every host needs to know all routes to every subnet. You always need to update the hosts’ routing tables once you add or remove a host to the cluster.</p></blockquote><blockquote><p>Every configuration in the diagram that is shown below the dashed line is handled by Docker: The docker0 bridge IP address configuration, the route to the Docker subnet on the host, the container IP addresses and the routes on the containers. The configuration above the line is up to the user and can be adapted to the individual environment.</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;老师的项目要在Ipv6环境下跑，结果发现中文版却并没有对此篇文章的翻译，本瓜皮就来翻译一下。&lt;br&gt;&lt;a href=&quot;https://docs.docker.com/engine/userguide/networking/default_network/ipv6/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;原文地址&lt;/a&gt;  &lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="Docker" scheme="http://yoursite.com/tags/Docker/"/>
    
      <category term="network" scheme="http://yoursite.com/tags/network/"/>
    
  </entry>
  
  <entry>
    <title>Elasticsearch-6.0.0在Centos 7.3上部署</title>
    <link href="http://yoursite.com/2017/12/31/elasticsearch%E5%9C%A8Centos%E4%B8%8A%E9%83%A8%E7%BD%B2/"/>
    <id>http://yoursite.com/2017/12/31/elasticsearch在Centos上部署/</id>
    <published>2017-12-31T07:23:56.000Z</published>
    <updated>2018-07-27T17:09:28.878Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>本文介绍了 elasticsearch 是什么以及如何在 Centos 上部署。 </p></div><a id="more"></a><h1 id="Elasticsearch简介"><a href="#Elasticsearch简介" class="headerlink" title="Elasticsearch简介"></a>Elasticsearch简介</h1><p>来自百度的介绍</p><p>ElasticSearch是一个基于Lucene的搜索服务器。它提供了一个分布式多用户能力的全文搜索引擎，基于RESTful web接口。Elasticsearch是用Java开发的，并作为Apache许可条款下的开放源码发布，是当前流行的企业级搜索引擎。设计用于云计算中，能够达到实时搜索，稳定，可靠，快速，安装使用方便。<br>我们建立一个网站或应用程序，并要添加搜索功能，但是想要完成搜索工作的创建是非常困难的。我们希望搜索解决方案要运行速度快，我们希望能有一个零配置和一个完全免费的搜索模式，我们希望能够简单地使用JSON通过HTTP来索引数据，我们希望我们的搜索服务器始终可用，我们希望能够从一台开始并扩展到数百台，我们要实时搜索，我们要简单的多租户，我们希望建立一个云的解决方案。因此我们利用Elasticsearch来解决所有这些问题以及可能出现的更多其它问题。</p><center><br>自己的理解<br></center><p>就是一个基于Lucene自带分布式的非常像Nosql的搜索引擎/结构化数据库(并不支持数据库的join操作)，基于HTTP协议通过 RESTful API, 传输Json数据提供稳定，可靠的搜索服务</p><h1 id="Elasticsearch-相关资料"><a href="#Elasticsearch-相关资料" class="headerlink" title="Elasticsearch 相关资料"></a>Elasticsearch 相关资料</h1><ul><li><a href="https://www.elastic.co/cn/" target="_blank" rel="noopener">elastic官网</a></li><li><a href="https://www.elastic.co/guide/index.html" target="_blank" rel="noopener">elastic文档</a></li><li><a href="https://www.elastic.co/guide/cn/index.html" target="_blank" rel="noopener">elastic中文文档</a></li><li><a href="http://www.code123.cc/docs/kibana-logstash/v4/index.html" target="_blank" rel="noopener">kibana指南</a></li><li><a href="https://github.com/medcl/elasticsearch-analysis-ik" target="_blank" rel="noopener">ik中文分词器</a></li></ul><h1 id="部署"><a href="#部署" class="headerlink" title="部署"></a>部署</h1><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><h3 id="Java安装"><a href="#Java安装" class="headerlink" title="Java安装"></a>Java安装</h3><p>通过yum进行安装,先搜索合适的版本</p><blockquote><p>yum search java|grep jdk</p></blockquote><p>选择版本安装</p><blockquote><p>yum install java-1.8.0-openjdk</p></blockquote><p>然后配置环境变量</p><h3 id="Elasticsearch安装"><a href="#Elasticsearch安装" class="headerlink" title="Elasticsearch安装"></a>Elasticsearch安装</h3><p>下载elasticsearch-6.0.0</p><blockquote><p>wget <a href="https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-6.0.0.tar.gz" target="_blank" rel="noopener">https://artifacts.elastic.co/downloads/elasticsearch/elasticsearch-6.0.0.tar.gz</a></p></blockquote><p>解压elasticsearch</p><blockquote><p>tar -zxvf elasticsearch-6.0.0.tar.gz</p></blockquote><p>做完这一步请先浏览下一节的elasticsearch配置，改变插件等安装的位置。</p><h3 id="Kibana安装"><a href="#Kibana安装" class="headerlink" title="Kibana安装"></a>Kibana安装</h3><p>kibana的安装方法与elasticsearch，注意版本号应与elasticsearch相同，kibana是一个可视化数据管理工具，你可以在服务器上安装开启服务进行管理，也可以在本地开启服务连接云主机，进行管理。不过重要的是kibana的版本号应与elasticsearch的版本号相同。</p><blockquote><p>wget <a href="https://artifacts.elastic.co/downloads/kibana/kibana-6.0.0-linux-x86_64.tar.gz" target="_blank" rel="noopener">https://artifacts.elastic.co/downloads/kibana/kibana-6.0.0-linux-x86_64.tar.gz</a></p></blockquote><blockquote><p>tar -zxvf kibana-6.0.0-linux-x86_64.tar.gz</p></blockquote><h3 id="ik分词器安装"><a href="#ik分词器安装" class="headerlink" title="ik分词器安装"></a>ik分词器安装</h3><p>先cd到elasticsearch目录下，然后通过elasticsearch-plugin安装</p><blockquote><p>./bin/elasticsearch-plugin install <a href="https://github.com/medcl/elasticsearch-analysis-ik/releases/download/v6.0.0/elasticsearch-analysis-ik-6.0.0.zip" target="_blank" rel="noopener">https://github.com/medcl/elasticsearch-analysis-ik/releases/download/v6.0.0/elasticsearch-analysis-ik-6.0.0.zip</a></p></blockquote><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><h3 id="Elasticsearch配置"><a href="#Elasticsearch配置" class="headerlink" title="Elasticsearch配置"></a>Elasticsearch配置</h3><p>elasticsearch致力于隐藏分布式复杂的特性，以下操作都是在底层自动完成的：</p><ul><li>将文档分区到不同的容器或者分片中，他们可以存在于一个或多个节点中</li><li>将分片均匀的分配到各个节点，对索引和搜索做负载均衡</li><li>冗余每个分片，防止硬件故障造成的数据丢失</li><li>将集群中任意一个节点上的请求路由到相应数据的所在节点</li><li>无论是增加节点，还是移除节点，分片都可以做到无缝的扩展和迁移</li></ul><p>喵，elasticsearch分布式的细节可以去研究官方文档，如何处理文档存储，如何进行扩展集群和故障转移，如何进行分布式搜索，分片是什么以及如何工作的。<del>当然这些都不懂也是可以使用elasticsearch的</del></p><p>要说elasticsearch的配置，得先搞清楚几个事情。</p><ul><li>集群(cluster)由一个或多个节点(node)组成，他们具有相同的cluster.name，它们协同工作，分享数据和负载。当加入新节点或者删除一个节点时，集群就会感知到并平衡数据。</li><li>节点(node) 一个Elasticsearch实例，一个机器可以有多个node,大多数情况node运行在一个独立的环境或虚拟机上</li><li>索引(index)这里的索引指的不是动词，es有两个index一个是动词建立索引，还有一个是名词，类似于关系型数据库的table。ES和RDB的对应关系如下：RDB =&gt; DB =&gt; Tables =&gt; Rows =&gt; Columns : ES =&gt; Indices =&gt; Types =&gt; Documents =&gt; Fields</li><li>分片(shard)ES是分布式搜索引擎，每个索引有一个或多个分片，索引的数据被分配到各个分片上，相当于一桶水用了N个杯子装，也利于横向扩展</li><li>集群中一个节点会被选举为主节点(master)，它将临时管理集群级别的一些变更，例如新建或删除索引，增加或移除节点等。主节点不参与文档级别的变更或搜索，这意味着在流量增长的时候，该主节点不会成为集群的瓶颈。</li><li>作为用户，我们能够与集群中的任何节点通信，包括主节点。每一个节点都知道文档存在于哪个节点上，它们可以转发请求到相应的节点上。我们访问的节点负责收集各节点返回的数据，最后一起返回给客户端。这一切都由Elasticsearch处理。</li></ul><hr><p>再来说说elasticsearch的配置文件elasticsearch.yml<br>elasticsearch默认启动的集群名字叫elasticsearch。为了安全，最好改个名字。</p><blockquote><p>cluster.name: elasticsearch_production</p></blockquote><p>然后是节点名字，默认会随机指定一个名字，elasticsearch作者增添了一些有趣的名字，可是这并不那么有趣，因为你会搞不清哪台物理机叫什么名字。最主要的是每次重启时，都会得到一个新的名字，这使得日志变得混乱，所以配置节点的名称是非常必要的。</p><blockquote><p>node.name: elasticsearch_001_data</p></blockquote><p>下面这个配置成节点的ip就好</p><blockquote><p>network.host: ip</p></blockquote><p>路径配置，这也是十分必要的，因为默认情况下，elasticsearch会把插件、日志以及数据放在安装目录下，重新安装就会很麻烦。可以通过逗号分隔指定多个目录，如果每个目录分别挂载不同的硬盘，这可是一个简单且高效实现一个软磁盘阵列的办法,和任何磁盘阵列一样一个硬盘坏了，数据也一样会丢失，而且elasticsearch不会将一个分片放在不同的硬盘上，因为一个驱动器的丢失会破坏所有的分片，所以elasticsearch还是将分片放在一个驱动器上。性能的话呢，如果想添加多个驱动器来提高一个单独索引的性能，帮助并不大，因为大多数节点只有一个分片和这样一个积极的驱动器。多个数据路径只是帮助有多个索引/分片再单个节点上<del>喵，如果需要更高级的、稳健的、灵活的配置，还是需要用software RAID软件，而不是多个数据路径的功能</del></p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">path.data:</span> <span class="string">/path/to/data1,/path/to/data2</span></span><br><span class="line"><span class="comment"># Path to log files:</span></span><br><span class="line"><span class="string">path.logs:</span> <span class="string">/path/to/logs</span></span><br><span class="line"><span class="comment"># Path to where plugins are installed:</span></span><br><span class="line"><span class="string">path.plugins:</span> <span class="string">/path/to/plugins</span></span><br></pre></td></tr></table></figure><p>最小主节点数<br>minimum_master_nodes 设定对你的集群的稳定 极其 重要。 当你的集群中有两个 masters（注：主节点）的时候，这个配置有助于防止 脑裂 ，一种两个主节点同时存在于一个集群的现象。</p><p>如果你的集群发生了脑裂，那么你的集群就会处在丢失数据的危险中，因为主节点被认为是这个集群的最高统治者，它决定了什么时候新的索引可以创建，分片是如何移动的等等。如果你有 两个 masters 节点， 你的数据的完整性将得不到保证，因为你有两个节点认为他们有集群的控制权。</p><p>这个配置就是告诉 Elasticsearch 当没有足够 master 候选节点的时候，就不要进行 master 节点选举，等 master 候选节点足够了才进行选举。</p><p>此设置应该始终被配置为 master 候选节点的法定个数（大多数个）。法定个数就是 ( master 候选节点个数 / 2) + 1 。 这里有几个例子：</p><p>如果你有 10 个节点（能保存数据，同时能成为 master），法定数就是 6 。<br>如果你有 3 个候选 master 节点，和 100 个 data 节点，法定数就是 2 ，你只要数数那些可以做 master 的节点数就可以了。<br>如果你有两个节点，你遇到难题了。法定数当然是 2 ，但是这意味着如果有一个节点挂掉，你整个集群就不可用了。 设置成 1 可以保证集群的功能，但是就无法保证集群脑裂了，像这样的情况，你最好至少保证有 3 个节点。<br>你可以在你的 elasticsearch.yml 文件中这样配置：</p><blockquote><p>discovery.zen.minimum_master_nodes: 2</p></blockquote><p>具体的还请阅读官方文档<del>有些部分还都不懂(逃</del>，还有<a href="http://blog.csdn.net/laigood/article/details/7421197" target="_blank" rel="noopener">配置文件的详解</a>,不过补充一个都没有提及的配置，如何开启外网ip访问？</p><blockquote><p>http.host: 0.0.0.0</p></blockquote><p>这样就能够通过ip加端口号进行访问了。</p><h3 id="Kibana配置"><a href="#Kibana配置" class="headerlink" title="Kibana配置"></a>Kibana配置</h3><p>kibana的配置文件在config目录下，这样就kibana就可以通过外网访问进行管理。<br><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">server.host:</span> <span class="number">0.0</span><span class="number">.0</span><span class="number">.0</span></span><br></pre></td></tr></table></figure></p><p>也可以通过下面这种方式在自己的pc上开启kibana来对ES进行管理<br><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">elasticsearch.url:</span> <span class="string">"http://ip:9200"</span></span><br></pre></td></tr></table></figure></p><h2 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h2><p>不能用root用户启动elasticsearch，ES由于安全考虑不能使用root权限启动,如果用root启动会报cannot run as root</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">groupadd elasticsearch</span><br><span class="line">useradd elastic -g elasticsearch</span><br><span class="line">chown -R -v elastic:elasticsearch elasticsearch-6.0.0</span><br><span class="line">su elastic</span><br><span class="line">./bin/elasticsearch -d</span><br></pre></td></tr></table></figure><p>如果你改变了配置文件中的各种路径，也要给那些路径配置权限，配置方法同上。</p><p>最后还要将 kernel 的 vm.max_map_count 设置为 262144。<br><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sysctl -w vm.max_map_count=262144</span><br></pre></td></tr></table></figure></p><p>这样就能够正确启动了，打开另外一个终端测试，如果得到类似的json格式的数据那么就说明搭建成功了</p><blockquote><p>curl ‘<a href="http://localhost:9200/?pretty&#39;" target="_blank" rel="noopener">http://localhost:9200/?pretty&#39;</a></p></blockquote><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"name"</span> : <span class="string">"2p_QR4W"</span>,</span><br><span class="line">  <span class="attr">"cluster_name"</span> : <span class="string">"elasticsearch_production"</span>,</span><br><span class="line">  <span class="attr">"cluster_uuid"</span> : <span class="string">"-Fgpx2fNRx64oiPXblNzOQ"</span>,</span><br><span class="line">  <span class="attr">"version"</span> : &#123;</span><br><span class="line">    <span class="attr">"number"</span> : <span class="string">"6.0.0"</span>,</span><br><span class="line">    <span class="attr">"build_hash"</span> : <span class="string">"8f0685b"</span>,</span><br><span class="line">    <span class="attr">"build_date"</span> : <span class="string">"2017-11-10T18:41:22.859Z"</span>,</span><br><span class="line">    <span class="attr">"build_snapshot"</span> : <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">"lucene_version"</span> : <span class="string">"7.0.1"</span>,</span><br><span class="line">    <span class="attr">"minimum_wire_compatibility_version"</span> : <span class="string">"5.6.0"</span>,</span><br><span class="line">    <span class="attr">"minimum_index_compatibility_version"</span> : <span class="string">"5.0.0"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">"tagline"</span> : <span class="string">"You Know, for Search"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>ES启动完毕后就可以启动kibana了，cd到kibana目录下</p><blockquote><p>./bin/kibana</p></blockquote><p>然后打开浏览器访问 </p><blockquote><p><a href="http://ip:5601/app/kibana" target="_blank" rel="noopener">http://ip:5601/app/kibana</a></p></blockquote><p>或者在本地启动kibana服务</p><blockquote><p><a href="http://localhost/app/kibana" target="_blank" rel="noopener">http://localhost/app/kibana</a></p></blockquote><p><del>懒得截图</del></p><h1 id="集群搭建"><a href="#集群搭建" class="headerlink" title="集群搭建"></a>集群搭建</h1><p>每台机子按照上述的配置文件进行配置，集群名相同，节点名字有序替换。</p><p>然后依次启动机子就好了。</p><p>本篇也会随着学习不断更新。</p>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;本文介绍了 elasticsearch 是什么以及如何在 Centos 上部署。 &lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="Elasticsearch" scheme="http://yoursite.com/tags/Elasticsearch/"/>
    
  </entry>
  
  <entry>
    <title>相对路径与绝对路径</title>
    <link href="http://yoursite.com/2017/08/13/%E7%9B%B8%E5%AF%B9%E8%B7%AF%E5%BE%84%E4%B8%8E%E7%BB%9D%E5%AF%B9%E8%B7%AF%E5%BE%84/"/>
    <id>http://yoursite.com/2017/08/13/相对路径与绝对路径/</id>
    <published>2017-08-13T14:45:35.000Z</published>
    <updated>2018-07-26T03:32:00.507Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>经常在页面中引用图片，html页面等，总结一下绝对路径与相对路径。<br>绝对路径很简单，相对路径的话同级文件夹下不需要在前面加../，其他情况都要加 </p></div><p><br><a id="more"></a></p><h3 id="若引用的资源和本身在同一路径下（既在同一目录下）"><a href="#若引用的资源和本身在同一路径下（既在同一目录下）" class="headerlink" title="若引用的资源和本身在同一路径下（既在同一目录下）"></a>若引用的资源和本身在同一路径下（既在同一目录下）</h3><pre><code>假设example.html的路径是D:/例子/example.html假设tupian.gif的路径是D:/例子/tupian.gif在example.html中引用tupian.gif 相对路径：&lt;img src=&quot;tupian.gif&quot; border=&quot;0&quot;/&gt; 绝对路径：&lt;img src=&quot;d:/例子/html/tupian.gif&quot; border=&quot;0&quot;/&gt; </code></pre><p>绝对的都差不多，只说相对路径。 </p><h3 id="要引用的文件在下一级文件夹下，文件名前加子文件夹名称"><a href="#要引用的文件在下一级文件夹下，文件名前加子文件夹名称" class="headerlink" title="要引用的文件在下一级文件夹下，文件名前加子文件夹名称"></a>要引用的文件在下一级文件夹下，文件名前加子文件夹名称</h3><p>   假设info.html路径是：c:\Inetpub\wwwroot\sites\blabla\info.html<br>   假设index.html路径是：c:\Inetpub\wwwroot\sites\blabla\html\tutorials\index.html<br>   在info.html加入index.html超链接的href应该这样写：html/tutorials/index.html </p><h3 id="要引用的文件在上一级文件夹下，文件名前加"><a href="#要引用的文件在上一级文件夹下，文件名前加" class="headerlink" title="要引用的文件在上一级文件夹下，文件名前加../"></a>要引用的文件在上一级文件夹下，文件名前加../</h3><pre><code>假设info.html路径是：c:\Inetpub\wwwroot\sites\blabla\info.html 假设index.html路径是：c:\Inetpub\wwwroot\sites\index.html 在info.html加入index.html超链接的代码应该这样写： &lt;a href=&quot;../index.html&quot;&gt;index.html&lt;/a&gt;</code></pre><p>举一反三：  ../表示源文件所在目录的上一级目录，../../表示源文件所在目录的上上级目录，以此类推 </p><h3 id="更复杂的情况："><a href="#更复杂的情况：" class="headerlink" title="更复杂的情况："></a>更复杂的情况：</h3><pre><code>假设info.html路径是：c:\Inetpub\wwwroot\sites\blabla\info.html 假设index.html路径是：c:\Inetpub\wwwroot\sites\html\index.html 在info.html加入index.html超链接的代码应该这样写：&lt;a href=&quot;../html/index.html&quot;&gt;index.html&lt;/a&gt; </code></pre><h3 id="最后一个例子"><a href="#最后一个例子" class="headerlink" title="最后一个例子"></a>最后一个例子</h3><pre><code>假设路径：D:\例子\html\style\view\pop.css D:\例子\html\images\view\tupian.gif  在css中引用这个图片：&lt;img src=&quot;../../images/view/tupian.gif&quot; border=&quot;0&quot;/&gt; </code></pre>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;经常在页面中引用图片，html页面等，总结一下绝对路径与相对路径。&lt;br&gt;绝对路径很简单，相对路径的话同级文件夹下不需要在前面加../，其他情况都要加 &lt;/p&gt;&lt;/div&gt;
&lt;p&gt;&lt;br&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="web" scheme="http://yoursite.com/tags/web/"/>
    
      <category term="Html" scheme="http://yoursite.com/tags/Html/"/>
    
  </entry>
  
  <entry>
    <title>Jsp笔记(一)</title>
    <link href="http://yoursite.com/2017/08/12/Jsp%E7%AC%94%E8%AE%B0(%E4%B8%80)/"/>
    <id>http://yoursite.com/2017/08/12/Jsp笔记(一)/</id>
    <published>2017-08-12T03:58:56.000Z</published>
    <updated>2018-07-26T03:29:33.440Z</updated>
    
    <content type="html"><![CDATA[<div class="note info"><p>小鱼笔记<br>Jsp基本语法  </p></div><a id="more"></a><h2 id="指令标识"><a href="#指令标识" class="headerlink" title="指令标识"></a>指令标识</h2><p>语法：</p><blockquote><p>&lt;%@ 指令名 属性 1=“属性1的值” 属性2=“属性2的值”%&gt;</p></blockquote><h3 id="指令名："><a href="#指令名：" class="headerlink" title="指令名："></a>指令名：</h3><ul><li>page 定义整个页面的相关属性</li><li>include 静态包含另外一个Jsp页面</li><li>taglib 自定义</li></ul><blockquote><p>&lt;%@ page import=”java.util.*”%&gt;</p></blockquote><blockquote><p>&lt;%@ include file =”path”%&gt;</p></blockquote><blockquote><p>&lt;%@ taglib %&gt;</p></blockquote><h2 id="表达式："><a href="#表达式：" class="headerlink" title="表达式："></a>表达式：</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;%= 表达式 %&gt;</span><br></pre></td></tr></table></figure><p>%与=之间不能有空格，但=后可以有<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">&lt;% String manager=<span class="string">"mr"</span>;%&gt;</span><br><span class="line">管理员： &lt;%= manager%&gt;</span><br></pre></td></tr></table></figure></p><h2 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h2><h3 id="html注释"><a href="#html注释" class="headerlink" title="html注释"></a>html注释</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;!--...--&gt;</span><br></pre></td></tr></table></figure><p>客户端可见，下载源代码</p><h3 id="隐藏注释："><a href="#隐藏注释：" class="headerlink" title="隐藏注释："></a>隐藏注释：</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;%-- ..... --%&gt;</span><br></pre></td></tr></table></figure><p>隐藏注释不能够从源代码中看到</p><h3 id="动态注释："><a href="#动态注释：" class="headerlink" title="动态注释："></a>动态注释：</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;!-- &lt;%=<span class="keyword">new</span> Date()%&gt; --&gt;</span><br></pre></td></tr></table></figure><h2 id="包含文件标识："><a href="#包含文件标识：" class="headerlink" title="包含文件标识："></a>包含文件标识：</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;jsp:include page=<span class="string">"url"</span> flush=<span class="string">"false|true"</span>/&gt;</span><br></pre></td></tr></table></figure><figure class="highlight plain"><figcaption><span>指令存在很大差别</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">1.  include指令通过file 属性指定被包含的文件，而且file属性不支持任何表达式，而jsp:include通过page属性指定被包含的文件，而且page属性支持JSP表达式。</span><br><span class="line">2.  include指令是先把被包含文件原封不动的插入到包含页中，然后再有JSP编译器编译成一个Java文件，使用Jsp:include时，分别编译</span><br><span class="line">3.  include 不能有重名的变量和方法 jsp:include中可以</span><br><span class="line"></span><br><span class="line">## 请求转发标识</span><br><span class="line">```Java</span><br><span class="line">&lt;jsp:forward page=&quot;url&quot;/&gt;</span><br></pre></td></tr></table></figure><h2 id="传递参数标识"><a href="#传递参数标识" class="headerlink" title="传递参数标识"></a>传递参数标识</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;jsp:param name=<span class="string">"参数名"</span> value=<span class="string">"参数值"</span>/&gt;</span><br></pre></td></tr></table></figure><p>例子<br><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&lt;jsp:forward page=<span class="string">"modify.jsp"</span>&gt;</span><br><span class="line">&lt;jsp:param name=<span class="string">"userid"</span> value=<span class="string">"7"</span>/&gt;</span><br><span class="line">&lt;/jsp:forward&gt;</span><br></pre></td></tr></table></figure></p><h2 id="9个JSP内置对象"><a href="#9个JSP内置对象" class="headerlink" title="9个JSP内置对象"></a>9个JSP内置对象</h2><h3 id="request"><a href="#request" class="headerlink" title="request"></a>request</h3><p>处理HTTP中的各项参数</p><h4 id="访问请求参数"><a href="#访问请求参数" class="headerlink" title="访问请求参数"></a>访问请求参数</h4><p>在页面中定义<br><figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;a href="delete.jsp?id=1&amp;user="&gt;删除&lt;/a&gt;</span><br></pre></td></tr></table></figure></p><p>在delete.jsp中获取参数的方法getParameter<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&lt;%</span><br><span class="line">String id  = request.getParameter(<span class="string">"id"</span>);</span><br><span class="line">String user = request.getParameter(<span class="string">"user"</span>);</span><br><span class="line">%&gt;</span><br></pre></td></tr></table></figure></p><p>id为1，user为空但不为null</p><h4 id="解决乱码"><a href="#解决乱码" class="headerlink" title="解决乱码"></a>解决乱码</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;%= <span class="keyword">new</span> String(request.getParameter(<span class="string">"name"</span>).getBytes(<span class="string">"ISO-8859-1"</span>)，<span class="string">"UTF-8"</span>)%&gt;</span><br></pre></td></tr></table></figure><p>所有的request的请求都是通过ISO-8859-1的，而在此页面采用的是UTF-8编码方式，通过上面这种方式可以防止乱码。</p><h4 id="作用域中管理属性"><a href="#作用域中管理属性" class="headerlink" title="作用域中管理属性"></a>作用域中管理属性</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">request.setAttribute(String name,Object obiect);</span><br></pre></td></tr></table></figure><p>name变量在request的范围内有效<br>对应方法<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">request.getAttribute(<span class="string">"String name"</span>);</span><br></pre></td></tr></table></figure></p><p>getAttribute()方法可以提取请求转发页面setAttribute()传的一些数据，然后对数据进行处理。</p><h4 id="获取cookie"><a href="#获取cookie" class="headerlink" title="获取cookie"></a>获取cookie</h4><p>getCookies()<br>getName()<br>getValue()<br>addCookie()</p><h4 id="获取客户端信息"><a href="#获取客户端信息" class="headerlink" title="获取客户端信息"></a>获取客户端信息</h4><p><img src="http://i4.nbimg.com/604261/32f861b1e9d9a03a.png" alt="requestscommon"></p><h4 id="显示国际化信息"><a href="#显示国际化信息" class="headerlink" title="显示国际化信息"></a>显示国际化信息</h4><p>request对象中的getLocale()和getLocales()方法允许获得本地语言<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">&lt;%</span><br><span class="line">Java.util.Locale locale=request.getLocale();</span><br><span class="line">String str = <span class="string">""</span>;</span><br><span class="line"><span class="keyword">if</span>(locale.equals(java.util.Locale.US))&#123;</span><br><span class="line">str=<span class="string">"Hello,welcome to access our company's web!"</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(locale.equals(java.util.Locale.CHINA))&#123;</span><br><span class="line">str=<span class="string">"您好，欢饮访问我们公司网站!"</span>;</span><br><span class="line">&#125;</span><br><span class="line">%&gt;</span><br><span class="line">&lt;%=str%&gt;</span><br></pre></td></tr></table></figure></p><h3 id="response"><a href="#response" class="headerlink" title="response"></a>response</h3><p>响应客户请求，向客户端输出信息</p><h4 id="重定向网页"><a href="#重定向网页" class="headerlink" title="重定向网页"></a>重定向网页</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">response.sendRedirect(String path);</span><br></pre></td></tr></table></figure><p>通过sendRedirect()方法将网页重定向到另一个页面，支持将地址重新定向到不同主机上，这一点与转发不同。在浏览器上重新得到网址，然后重新发送请求链接。重定向操作后，request中的属性全部失效，并且开始一个新的request对象</p><h4 id="处理HTTP开头"><a href="#处理HTTP开头" class="headerlink" title="处理HTTP开头"></a>处理HTTP开头</h4><h5 id="禁用缓存"><a href="#禁用缓存" class="headerlink" title="禁用缓存"></a>禁用缓存</h5><p>适用于一些安全性较高的网站<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&lt;%</span><br><span class="line">response.setHeader(<span class="string">"Cache-Control"</span>,<span class="string">"no-store"</span>);</span><br><span class="line">response.setDateHeader(<span class="string">"Expires"</span>,<span class="number">0</span>);</span><br><span class="line">%&gt;</span><br></pre></td></tr></table></figure></p><h5 id="页面自动刷新"><a href="#页面自动刷新" class="headerlink" title="页面自动刷新"></a>页面自动刷新</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&lt;%</span><br><span class="line">response.setHeader(<span class="string">"refresh"</span>,<span class="string">"10"</span>);</span><br><span class="line">%&gt;</span><br></pre></td></tr></table></figure><p>每隔十秒钟刷新一次</p><h5 id="定时跳转网页"><a href="#定时跳转网页" class="headerlink" title="定时跳转网页"></a>定时跳转网页</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&lt;%</span><br><span class="line">response.setHeader(<span class="string">"refresh"</span>,<span class="string">"5;URL=login.jsp"</span>);</span><br><span class="line">%&gt;</span><br></pre></td></tr></table></figure><p>5秒后跳向指定网页</p><h4 id="设置输出缓冲"><a href="#设置输出缓冲" class="headerlink" title="设置输出缓冲"></a>设置输出缓冲</h4><p>只有满足一下三种情况之一才会把缓冲区的内容写到客户端</p><ol><li>Jsp页面的输出信息已经全部写入到了缓冲区</li><li>缓冲区已满</li><li>在Jsp页面中，调用了response对象的flushBuffer()方法或out对象的flush()方法</li></ol><p>response中对缓冲区进行配置的方法<br><img src="http://i4.nbimg.com/604261/3bbaf6af14934335.png" alt="response"></p><h3 id="session"><a href="#session" class="headerlink" title="session"></a>session</h3><p>通过session可以在应用程序的Web页面间进行跳转时，保存用户的状态，使整个用户会话会一直存在下去，直到关闭浏览器，例如，Tomcat服务器默认为30分钟，不过这个时间可以通过编写程序进行修改</p><h4 id="创建及获取客户的会话"><a href="#创建及获取客户的会话" class="headerlink" title="创建及获取客户的会话"></a>创建及获取客户的会话</h4><p>将信息保存在session范围内<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">session.setAttribute().setAttribute(String name,Object obj)</span><br></pre></td></tr></table></figure></p><p>获取保存在session范围内的信息<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">session.getAttribute(String name)</span><br></pre></td></tr></table></figure></p><p>返回的是Object类型，所以要添加toString()方法或强制转换<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">String user=(String)session.getAttribute(<span class="string">"username"</span>);</span><br><span class="line">String user1=session.getAttribute(<span class="string">"username"</span>).toString();</span><br></pre></td></tr></table></figure></p><h4 id="从会话中移动指定的绑定对象"><a href="#从会话中移动指定的绑定对象" class="headerlink" title="从会话中移动指定的绑定对象"></a>从会话中移动指定的绑定对象</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">session.removeAttribute(String name)</span><br></pre></td></tr></table></figure><h4 id="销毁session"><a href="#销毁session" class="headerlink" title="销毁session"></a>销毁session</h4><p>虽然session对象过一段时间会自动消失，但对于某些实时统计在线人数的网站（例如聊天室）,所以要手动销毁session来统计准确人数<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">session.invalidate();</span><br></pre></td></tr></table></figure></p><h4 id="会话超时管理"><a href="#会话超时管理" class="headerlink" title="会话超时管理"></a>会话超时管理</h4><p>如果用户超过session存在时间后操作，会报错，为了避免这种现象，所以要对session的有效性进行判断</p><table><thead><tr><th style="text-align:center">方法</th><th style="text-align:center">描述</th></tr></thead><tbody><tr><td style="text-align:center">getlastAccessedTime()</td><td style="text-align:center">返回客户端最后一次与会话相关联的请求时间</td></tr><tr><td style="text-align:center">getMaxInactiveInterval()</td><td style="text-align:center">以秒为单位返回一个会话内两个请求最大的时间间隔</td></tr><tr><td style="text-align:center">setMaxInactiveInterval()</td><td style="text-align:center">以秒为单位设置session的有效时间</td></tr></tbody></table><h3 id="application"><a href="#application" class="headerlink" title="application"></a>application</h3><p>application 用于保存所有应用程序中的公有数据。它在服务器启动时自动创建，在服务器停止时销毁。比session的生命周期更长</p><h4 id="访问应用程序初始化参数"><a href="#访问应用程序初始化参数" class="headerlink" title="访问应用程序初始化参数"></a>访问应用程序初始化参数</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">application.getInitParameter(String name);</span><br></pre></td></tr></table></figure><p>返回已命名的参数值<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">application.getInitParameterNames();</span><br></pre></td></tr></table></figure></p><p>返回所有已定义的应用程序初始化参数名的枚举<br>实例：应用getAttributeNames()方法获取web.xml中定义的全部应用程序初始化参数名，并循环输出<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&lt;%@ page <span class="keyword">import</span>=<span class="string">"java.util.*"</span>%&gt;</span><br><span class="line">&lt;%</span><br><span class="line">Enumeration enema = application.getInitParameterNames();</span><br><span class="line"><span class="keyword">while</span>(enema.hasMoreElements())&#123;</span><br><span class="line">String name = (String)enema.nextElement();</span><br><span class="line">    String value = application.getInitPatameter(name);</span><br><span class="line">    out.println(name+<span class="string">":"</span>);</span><br><span class="line">    out.println(value);</span><br><span class="line">&#125;</span><br><span class="line">%&gt;</span><br></pre></td></tr></table></figure></p><h4 id="管理应用程序环境属性"><a href="#管理应用程序环境属性" class="headerlink" title="管理应用程序环境属性"></a>管理应用程序环境属性</h4><table><thead><tr><th style="text-align:center">方法</th><th style="text-align:center">描述</th></tr></thead><tbody><tr><td style="text-align:center">getAttributeNames()</td><td style="text-align:center">获取所有application对象使用的属性名</td></tr><tr><td style="text-align:center">getAttribute(String name)</td><td style="text-align:center">从application对象中获取指定对象名</td></tr><tr><td style="text-align:center">getAttribute(String key,Object obj)</td><td style="text-align:center">使用指定名称和指定对象在application对象中进行关联</td></tr><tr><td style="text-align:center">removeAttribute(String name)</td><td style="text-align:center">从application对象中去掉指定名称的属性</td></tr></tbody></table><h3 id="out"><a href="#out" class="headerlink" title="out"></a>out</h3><p>out对象用于在web浏览器内输出信息，并且管理应用服务器上的输出缓冲区</p><h4 id="print-方法"><a href="#print-方法" class="headerlink" title="print()方法"></a>print()方法</h4><p>向客户端浏览器输出信息。通过该方法向客户端浏览器输出信息与使用JSP表达式输出信息相同<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&lt;%</span><br><span class="line">out.print(<span class="string">"明日科技"</span>)</span><br><span class="line">%&gt;</span><br><span class="line">&lt;%= <span class="string">"明日科技"</span>%&gt;</span><br></pre></td></tr></table></figure></p><h4 id="println-方法"><a href="#println-方法" class="headerlink" title="println()方法"></a>println()方法</h4><p>与print方法不同，在输出内容后，会输出一个换行符</p><h4 id="管理影响缓冲"><a href="#管理影响缓冲" class="headerlink" title="管理影响缓冲"></a>管理影响缓冲</h4><p>out对象比较重要的一个功能<br><img src="http://i1.fuimg.com/604261/8e5034d627ea5bef.png" alt="outmethod"></p><h3 id="pageContext"><a href="#pageContext" class="headerlink" title="pageContext"></a>pageContext</h3><p>获取会话范围，通过它可以获取JSP页面的request,session,application,exception<br><img src="http://i1.fuimg.com/604261/5c23e49b61adb79f.png" alt="pageContextmethod1"><br><img src="http://i1.fuimg.com/604261/63741961d62404e8.png" alt="pageContextmethod2"></p><h3 id="config"><a href="#config" class="headerlink" title="config"></a>config</h3><p>读取web.xml配置信息的config对象，通过getServletConfig()方法可以获取一个config对象，当以个Servlet初始化时，容器吧某些信息通过config对象传给这个Servlet<br><img src="http://i2.fuimg.com/604261/03f324259eaaacc1.png" alt="configmethod"></p><h3 id="page"><a href="#page" class="headerlink" title="page"></a>page</h3><p>应答或请求page对象，可以用来当作this关键词的别名<br><img src="http://i2.fuimg.com/604261/243886cd68422ab6.png" alt="pagemethod"></p><h3 id="exception"><a href="#exception" class="headerlink" title="exception"></a>exception</h3><p>获取异常信息，只有在page指令中设置为isErrorPage属性值为true的页面中才可以被使用，在一般的JSP页面中使用该对象无法编译JSP文件。<br><img src="http://i4.fuimg.com/604261/080ab4b03bc47c7b.png" alt="exceptionmethod"></p>]]></content>
    
    <summary type="html">
    
      &lt;div class=&quot;note info&quot;&gt;&lt;p&gt;小鱼笔记&lt;br&gt;Jsp基本语法  &lt;/p&gt;&lt;/div&gt;
    
    </summary>
    
      <category term="Material" scheme="http://yoursite.com/categories/Material/"/>
    
    
      <category term="Java" scheme="http://yoursite.com/tags/Java/"/>
    
      <category term="web" scheme="http://yoursite.com/tags/web/"/>
    
      <category term="Jsp" scheme="http://yoursite.com/tags/Jsp/"/>
    
  </entry>
  
  <entry>
    <title>写在无聊前</title>
    <link href="http://yoursite.com/2017/08/03/%E5%86%99%E5%9C%A8%E6%97%A0%E8%81%8A%E5%89%8D/"/>
    <id>http://yoursite.com/2017/08/03/写在无聊前/</id>
    <published>2017-08-03T09:50:17.000Z</published>
    <updated>2018-07-27T16:59:32.439Z</updated>
    
    <content type="html"><![CDATA[<p><img src="https://wallpapers.wallhaven.cc/wallpapers/full/wallhaven-365216.jpg" alt="mario"></p><center><br><br>沉迷Dota2，再加上拖延症，我的瓜皮小站终于在8月3日上线了。<br><br>在这里会整理积累自己做的东西，发发牢骚和记笔记，技术贴怕是不会有了。希望这个博客能够一直更新下去吧！<br><br></center>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;&lt;img src=&quot;https://wallpapers.wallhaven.cc/wallpapers/full/wallhaven-365216.jpg&quot; alt=&quot;mario&quot;&gt;&lt;/p&gt;
&lt;center&gt;&lt;br&gt;&lt;br&gt;沉迷Dota2，再加上拖延症，我的瓜皮小站终于在
      
    
    </summary>
    
      <category term="Complaint" scheme="http://yoursite.com/categories/Complaint/"/>
    
    
      <category term="碎碎念" scheme="http://yoursite.com/tags/%E7%A2%8E%E7%A2%8E%E5%BF%B5/"/>
    
  </entry>
  
</feed>
